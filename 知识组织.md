# 全学段、跨学科统一知识地图设计框架（V2）

本文用于替代“以教材知识单元为中心”的旧版描述方式，转向“以稳定知识主干为中心”的统一知识地图设计。目标不是简单汇总各学科教材内容，而是建立一套可跨学科、跨学段、跨版本复用的知识本体与知识网络。

---

## 一、定位调整：从“教材知识切分”到“统一知识地图”

旧框架的优点是便于整理教材内容，但如果最终目标是构建全学段、跨学科、统一的知识体系，还需要做三点调整：

1. 把“教材中的知识单元”改为“跨教材稳定存在的 canonical knowledge node”。
2. 把“学科、学段、教材版本”从知识核心定义中拆出去，单独作为课程画像与出处信息。
3. 把“历史、案例、评估、反思、题型”从知识本体中拆出去，作为节点卡与教学扩展信息。

因此，统一知识地图的主体不应是“课本章节”，也不应是“某一学科当下的教学说法”，而应是：

- 可独立定义
- 可跨教材复用
- 可建立稳定关系
- 可在不同学段中逐步加深的知识节点

---

## 二、总体设计原则

### 2.1 主干优先

先建立稀疏、稳定、可解释的知识主干网络，再扩展节点卡、证据、题目、案例和教学活动。

### 2.2 本体与教学分层

知识本体回答“它是什么”；教学画像回答“在哪学、学到什么程度”；节点卡回答“如何展开说明”；证据层回答“出处在哪”。

### 2.3 图原生表达

前置关系、因果关系、组成关系、表征关系、应用关系应显式建边，而不是只写在文本字段里。

### 2.4 学科中立

同一个 canonical node 可以服务多个学科、多个学段、多个教材版本。学科与年级不应该写死在节点核心定义中。

### 2.5 粒度保守

主干图只保留“足够稳定、足够通用、足够可连接”的节点。教材细节、例子、局部表述优先下沉到节点卡和证据层。

---

## 三、四层统一架构

| 层级 | 名称 | 主要回答的问题 | 典型对象 | 是否跨学科稳定 |
|------|------|----------------|----------|----------------|
| Layer A | Canonical Backbone | 这个知识点本身是什么？与什么知识有关？ | 节点、关系 | 是 |
| Layer B | Curriculum Profile | 在什么学段、学科、版本中出现？要求掌握到什么程度？ | 课标映射、学段画像、教材画像 | 部分稳定 |
| Layer C | Node Card | 这个节点如何展开说明？常见误区、案例、应用有哪些？ | 节点卡、section、扩展说明 | 否，按节点类型定制 |
| Layer D | Provenance & Evidence | 证据来自哪里？出现在哪一页、哪一课、哪一段？ | mentions、evidence、目录锚点 | 否，强依赖来源 |

### Layer A：Canonical Backbone

这是统一知识地图的核心。只保留稳定知识节点和稳定关系。

适合放在这一层的内容：

- 概念
- 物质/实体
- 过程
- 原理/规律
- 方法
- 技能
- 表征
- 事件
- 议题

不适合直接放在这一层的内容：

- 某一页中的例句
- 某一道具体例题
- 某次实验中的局部观察记录
- 某一教材专属说法

### Layer B：Curriculum Profile

这一层负责表达“一个稳定知识点在不同学段中的出现方式”。

适合放在这一层的字段：

- `subject`
- `school_stage`
- `grade_band`
- `curriculum_role`
  例如：首次引入、巩固深化、综合应用、迁移拓展
- `mastery_level`
  例如：识记、理解、应用、建模、迁移
- `framework_refs`
- `textbook_refs`

### Layer C：Node Card

这一层负责“详细说明”，而不是定义 canonical node。

适合放在这一层的 section：

- 定义
- 本质理解
- 核心要点
- 操作步骤
- 应用场景
- 常见误区
- 典型例子
- 评估提示
- 拓展方向
- 相关证据

像“历史发展”“证明推导”“局限性”“学习反思”这类内容应作为可选 section，而不是要求所有节点都具备的固定层。

### Layer D：Provenance & Evidence

这一层负责可追溯性。

包括：

- 教材目录锚点
- mentions
- evidence
- 页面位置
- 原句摘录
- 提取方法

这层是为了回答“为什么这个节点成立”“它在教材里从哪里来”，而不是为了定义知识本体。

---

## 四、统一分类体系：两条正交轴

旧版把“事实性/概念性/程序性/元认知”作为主分类，这更适合表达学习方式，而不是知识对象类型。统一知识地图建议使用两条轴：

### 4.1 轴一：知识对象类型 `node_kind`

这是主轴，用于定义“这个节点本体上是什么”。

| 一级类型 | 说明 | 常见子类/示例 |
|----------|------|---------------|
| `entity` | 可被识别、命名、区分的对象 | 物质、生物、地理单元、人物、制度对象 |
| `concept` | 抽象概念、类别、定义 | 函数、混合物、修辞、民主 |
| `process` | 变化过程、作用过程、演化过程 | 光合作用、蒸发、工业化、阅读理解过程 |
| `principle` | 原理、定律、规律、机制 | 质量守恒、杠杆原理、语法规则 |
| `method` | 解决问题或完成任务的方法 | 燃烧法、配方法、史料分析法 |
| `skill` | 可训练、可评估的能力 | 实验基本操作、写作技能、地图判读 |
| `representation` | 表征与符号系统 | 元素符号、化学式、函数图像、时间轴 |
| `activity` | 教学活动或探究活动 | 实验、调查、写作任务、讨论活动 |
| `event` | 特定历史或现实事件 | 鸦片战争、工业革命、火山喷发 |
| `issue` | 需要讨论的现实议题 | 空气污染、资源循环、伦理问题 |

说明：

- 当前项目中的 `substance` 可以视为 `entity` 的细分。
- 当前项目中的 `experiment` 更适合作为 `activity` 的细分。
- 当前项目中的 `symbol` 可视为 `representation` 的细分。
- 当前项目中的 `question` 更偏教学任务层，不建议默认进入 canonical backbone，除非它已抽象为稳定的研究问题或思维问题。

### 4.2 轴二：学习方式 `learning_mode`

这一轴保留旧版的优点，但作为辅助标签，不作为主节点类型。

| 学习方式 | 说明 | 典型用途 |
|----------|------|----------|
| `factual` | 以识记和辨认为主 | 名称、符号、常量、时间地点 |
| `conceptual` | 以理解概念和关系为主 | 定义、分类、原理、结构 |
| `procedural` | 以操作和步骤为主 | 计算、实验、作答、制作 |
| `metacognitive` | 以策略和反思为主 | 学习方法、解题策略、迁移意识 |

一个节点可以同时具有多个 `learning_mode`。

例如：

- “氧气”是 `entity`，学习方式偏 `factual + conceptual`
- “燃烧法”是 `method`，学习方式偏 `procedural`
- “实验误差分析”是 `concept + method`，学习方式偏 `conceptual + metacognitive`

---

## 五、Canonical Node 的统一定义

统一知识地图中的最小主干单位建议定义为：

> 一个可以被独立命名、独立定义、独立建立关系，并能在多个学习情境中重复复用的知识节点。

### 5.1 主干节点应包含的核心字段

| 字段 | 作用 | 是否建议进入主干层 |
|------|------|-------------------|
| `id` | 稳定唯一标识 | 是 |
| `canonical_name` | 规范名称 | 是 |
| `node_kind` | 知识对象类型 | 是 |
| `aliases` | 同义名、别名、常见教材说法 | 是 |
| `definition` | 1 到 2 句精确定义 | 是 |
| `properties` | 最核心的结构属性 | 是 |
| `relations` | 通过边表达，而不是塞进文本字段 | 是 |
| `framework_refs` | 课程标准映射 | 建议放画像层，也可保留引用 |
| `subject` | 所属学科 | 不建议放主干核心 |
| `grade_level` | 适用年级 | 不建议放主干核心 |
| `examples` | 典型例子 | 不建议放主干核心 |
| `common_errors` | 常见误区 | 不建议放主干核心 |

### 5.2 不建议写死在主干节点里的内容

- 某一版本教材中的专有措辞
- 某一年级独有的教学深度
- 具体题目
- 具体课时安排
- 某一证明过程的完整文本
- 某一实验的局部观察记录

---

## 六、关系系统设计

统一知识地图不能只靠节点，还要靠稳定关系类型。建议按“关系家族”来组织。

### 6.1 分类/组成关系

| 关系 | 含义 | 示例 |
|------|------|------|
| `is_a` | A 是 B 的一种 | 氧气 `is_a` 物质 |
| `part_of` | A 是 B 的组成部分 | 氧气 `part_of` 空气组成 |
| `contains` | A 包含 B | 空气 `contains` 氧气 |
| `instance_of` | A 是某类对象的实例 | 鸦片战争 `instance_of` 历史事件 |

### 6.2 依赖/进阶关系

| 关系 | 含义 | 示例 |
|------|------|------|
| `prerequisite_for` | A 是学习 B 的前提 | 分数 `prerequisite_for` 方程 |
| `depends_on` | B 的理解依赖 A | 空气中氧气含量测定 `depends_on` 燃烧法 |
| `extends` | B 是 A 的扩展 | 一次函数 `extends` 函数 |

### 6.3 语义/因果关系

| 关系 | 含义 | 示例 |
|------|------|------|
| `explains` | A 用于解释 B | 质量守恒解释化学反应中质量变化 |
| `causes` | A 导致 B | 大气污染物增加 `causes` 空气质量下降 |
| `affects` | A 影响 B | 温度 `affects` 溶解度 |
| `has_property` | A 具有某性质 | 氮气 `has_property` 化学性质不活泼 |

### 6.4 操作/应用关系

| 关系 | 含义 | 示例 |
|------|------|------|
| `uses` | A 使用 B | 燃烧法 `uses` 红磷 |
| `measures` | A 测量 B | 氧气含量测定实验 `measures` 空气中氧气比例 |
| `produces` | A 生成 B | 镁燃烧 `produces` 氧化镁 |
| `consumes` | A 消耗 B | 红磷燃烧 `consumes` 氧气 |
| `applies_to` | A 适用于 B | 配方法 `applies_to` 一元二次方程 |

### 6.5 表征/跨学科关系

| 关系 | 含义 | 示例 |
|------|------|------|
| `represented_by` | A 可以被 B 表示 | 氧气 `represented_by` O2 |
| `symbolizes` | B 是 A 的符号表征 | 元素 `symbolized_by` 元素符号 |
| `analogous_to` | A 与 B 在结构上相似 | 电路系统 `analogous_to` 水流系统 |
| `same_as` | A 与 B 语义等价 | 氧气 `same_as` oxygen |

说明：

- 当前项目已有的 `contains / explains / uses / measures / produces / consumes / part_of / prerequisite_for / same_as / related_to` 可以保留。
- 为了支持跨学科知识地图，建议后续增补 `is_a / depends_on / causes / affects / has_property / represented_by / analogous_to / applies_to`。

---

## 七、知识点抽取粒度：三层粒度模型

粒度是统一知识体系最关键的设计点之一。建议采用三层模型，而不是把所有内容都抽成 node。

### 7.1 G1：Backbone Node

这是主干节点层。

进入这一层的对象必须满足：

1. 可以用 1 到 2 句话独立定义。
2. 能和至少 2 个其他节点建立稳定关系。
3. 会在多个页面、多个单元、多个任务或多个学段中复现。
4. 可以成为明确的教学目标或评估点。
5. 不只是某个例子、某个数据、某句教材原话。

### 7.2 G2：Micro-claim / Card Section

这是节点卡展开层，适合表达：

- 核心性质
- 局部判断点
- 操作步骤
- 常见误区
- 典型例子
- 应用场景

这些内容对理解节点很重要，但不一定都值得进入主干图。

### 7.3 G3：Evidence / Example / Exercise

这一层是证据与教学材料层，包括：

- 教材原句
- 图示说明
- 例题
- 实验观察
- 史料片段
- 阅读材料

这些内容用于支撑和追溯，不建议直接抽成 canonical node。

---

## 八、粒度判断示例

### 8.1 化学

| 内容 | 建议层级 | 原因 |
|------|----------|------|
| 氧气 | G1 主干节点 | 可独立定义、跨教材复现、可连多条边 |
| 空气的组成 | G1 主干节点 | 是核心概念，可连接空气、氮气、氧气、混合物 |
| 氧气约占空气体积 21% | G2 节点卡内容 | 是关键属性，但本身不必单独成主干节点 |
| 拉瓦锡实验的具体叙述 | G3 证据 | 是来源片段，不是本体节点 |

### 8.2 数学

| 内容 | 建议层级 | 原因 |
|------|----------|------|
| 函数 | G1 主干节点 | 跨学段核心概念 |
| 一次函数 | G1 主干节点 | 稳定子概念 |
| 图像平移的三种情况 | G2 节点卡内容 | 是某节点的展开规律 |
| 某教材中的一道例题 | G3 证据/练习 | 具体题目不宜进入主干 |

### 8.3 语文

| 内容 | 建议层级 | 原因 |
|------|----------|------|
| 比喻 | G1 主干节点 | 可独立定义、可跨年级复用 |
| 比喻的表达效果 | G2 节点卡内容 | 是该知识点的分析维度 |
| 某篇课文中的一句比喻句 | G3 证据/例子 | 是例句，不是本体节点 |

### 8.4 历史

| 内容 | 建议层级 | 原因 |
|------|----------|------|
| 工业革命 | G1 主干节点 | 稳定历史事件/过程节点 |
| 工业革命对城市化的影响 | G2 节点卡内容 | 是影响分析 |
| 某幅史料图片 | G3 证据 | 是材料来源 |

---

## 九、学科与学段应如何表达

如果目标是“全学段、跨学科、统一知识地图”，建议把 `subject` 和 `grade_level` 从主干层拆出，放到课程画像层。

### 9.1 Curriculum Profile 建议字段

| 字段 | 作用 |
|------|------|
| `subject` | 学科归属 |
| `school_stage` | 学段，如小学/初中/高中 |
| `grade_band` | 年级带，如 1-2、3-4、7-9 |
| `curriculum_role` | 首次引入、深化、迁移、综合应用 |
| `mastery_level` | 知道、理解、运用、建模、迁移 |
| `framework_refs` | 对应课标条目 |
| `textbook_refs` | 对应教材与单元 |

### 9.2 一个节点可以有多个画像

例如“氧气”可以同时拥有：

- 初中化学画像：知道氧气在空气中的体积分数、能支持燃烧
- 高中化学画像：理解氧化还原、电子转移等更深层性质
- 生物画像：理解呼吸作用中氧的意义
- 环境画像：理解空气质量与氧含量、污染之间的关系

这说明“知识节点”应稳定，“学段画像”应变化。

---

## 十、Node Card 的推荐结构

Node Card 不应强制所有节点都使用同一模板，而应按节点类型配置 pattern。

### 10.1 概念类

- 定义
- 本质理解
- 核心要点
- 易混辨析
- 典型例子
- 后续连接

### 10.2 实体类

- 基本身份
- 关键属性
- 组成/结构
- 用途与功能
- 检验与区分
- 常见误解

### 10.3 过程/原理类

- 过程描述
- 条件与机制
- 输入与输出
- 影响因素
- 典型现象
- 相关证据

### 10.4 方法/技能类

- 目标
- 适用条件
- 操作步骤
- 判断依据
- 常见错误
- 迁移应用

### 10.5 事件/议题类

- 核心定义
- 背景
- 关键因素
- 影响
- 多视角理解
- 现实关联

说明：

- “历史发展”“证明推导”“评估标准”“元认知提示”都可以保留，但应作为按需展开的 section。
- 这些内容更适合放在 Node Card，而不是进入统一主干 schema。

---

## 十一、从零开始构建统一知识体系的推荐流程

### Phase 1：定义标准

- 确定 `node_kind`
- 确定 `learning_mode`
- 确定 edge 类型
- 确定粒度准则

### Phase 2：建立课程参考框架

- 从课标抽取 `domain -> topic -> expectation`
- 形成 curriculum framework

### Phase 3：建立来源锚点

- 抽教材目录、单元、课题、活动、复习
- 形成 outline

### Phase 4：抽取知识主干

- 以 lesson 或小范围内容为单位抽 canonical nodes
- 同步抽 edges、mentions、evidence

### Phase 5：归一化

- 合并重复节点
- 统一别名
- 收敛关系类型
- 校正粒度

### Phase 6：QA

- 查重
- 查孤立节点
- 查无证据节点
- 查坏引用
- 查关系混乱

### Phase 7：扩展节点卡

- 只对高价值节点生成 Node Card
- 按 pattern 自动展开

### Phase 8：导出知识图谱

- 当 backbone、profile、card、provenance 都稳定后，再导出为图数据库或 RDF/OWL 结构

---

## 十二、对当前项目的直接建议

### 12.1 保留现有分层方向

当前项目中的：

- `node`
- `edge`
- `node_card`
- `mention`
- `evidence`

这五类对象已经非常接近统一知识地图的正确分层，应继续保留。

### 12.2 节点类型建议升级为“一级类型 + 子类型”

例如：

- `entity/substance`
- `activity/experiment`
- `representation/symbol`
- `concept`
- `method`
- `skill`

这样更适合跨学科扩展。

### 12.3 布鲁姆分类下沉为学习画像

“事实性/概念性/程序性/元认知”建议作为 `learning_mode`，不要直接替代 `node_kind`。

### 12.4 学科与年级下沉为 curriculum profile

不要把 `subject` 和 `grade_level` 作为节点核心标识，否则后续跨学科和跨学段合并会非常困难。

### 12.5 主干粒度保持保守

优先抽：

- 核心概念
- 核心实体
- 核心方法
- 核心过程
- 核心原理

暂缓抽：

- 细碎例题
- 教师提示语
- 局部判断句
- 一次性教材说法

---

## 十三、统一知识地图的最小可行定义

如果要用一句话概括本框架，可以定义为：

> 统一知识地图 = 稳定知识主干（Backbone） + 学段学科画像（Profile） + 节点扩展说明（Node Card） + 可追溯出处（Provenance）

其中：

- Backbone 负责统一
- Profile 负责差异
- Node Card 负责展开
- Provenance 负责可信

---

## 十四、结论

旧版框架适合“整理教材知识内容”，新版框架更适合“构建统一知识地图”。

最关键的变化不是多加几个字段，而是做出三项结构性区分：

1. 区分“知识本体”与“教学画像”
2. 区分“主干节点”与“扩展说明”
3. 区分“知识定义”与“教材证据”

只有这样，知识结构才能真正跨学科、跨教材、跨学段地生长，最终形成可计算、可导航、可扩展的全学段知识地图。
