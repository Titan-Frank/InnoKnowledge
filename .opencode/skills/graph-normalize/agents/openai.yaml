interface:
  display_name: "Graph Normalize"
  short_description: "Deduplicate and normalize textbook graph data"
  default_prompt: "Deduplicate nodes, normalize aliases, and resolve relation proposals conservatively without deleting existing cross-stage profiles or auto-overwriting conflicting canonical edges unless the user explicitly requests it."
