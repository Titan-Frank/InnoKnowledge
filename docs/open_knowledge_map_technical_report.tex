% Open Knowledge Map technical report.
% Compile from the repository root with: cd docs && latexmk -xelatex -interaction=nonstopmode -halt-on-error open_knowledge_map_technical_report.tex
\documentclass[11pt,letterpaper]{article}

\usepackage{iftex}
\ifPDFTeX
  \usepackage[utf8]{inputenc}
  \usepackage[T1]{fontenc}
  \usepackage{lmodern}
  \usepackage{CJKutf8}
\else
  \usepackage{fontspec}
  \usepackage{xeCJK}
  \IfFontExistsTF{Songti SC}{
    \setCJKmainfont{Songti SC}
    \setCJKmonofont{Heiti SC}
  }{
    \IfFontExistsTF{Noto Serif CJK SC}{
      \setCJKmainfont{Noto Serif CJK SC}
      \setCJKmonofont{Noto Sans Mono CJK SC}
    }{
      \setCJKmainfont[
        Path=/usr/local/texlive/2024/texmf-dist/fonts/opentype/public/fandol/,
        BoldFont=FandolSong-Bold.otf
      ]{FandolSong-Regular.otf}
      \setCJKmonofont[
        Path=/usr/local/texlive/2024/texmf-dist/fonts/opentype/public/fandol/
      ]{FandolHei-Regular.otf}
    }
  }
\fi
\usepackage{microtype}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{array}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{enumitem}
\usepackage{graphicx}
\usepackage{tikz}
\usepackage{listings}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{float}
\usepackage{titling}

\usetikzlibrary{arrows.meta,positioning,shapes.geometric,fit,calc}

\geometry{
  left=1.05in,
  right=1.05in,
  top=1.0in,
  bottom=1.0in
}

\hypersetup{
  colorlinks=true,
  linkcolor=blue!45!black,
  citecolor=blue!45!black,
  urlcolor=blue!45!black,
  pdftitle={Open Knowledge Map: An AI-Native Knowledge Object Infrastructure for Evidence-Grounded Learning Systems},
  pdfauthor={Wenbo Wu and Aimin Zhou}
}

\setlist[itemize]{leftmargin=1.25em,itemsep=0.25em,topsep=0.35em}
\setlist[enumerate]{leftmargin=1.5em,itemsep=0.25em,topsep=0.35em}
\setlength{\parskip}{0.55em}
\setlength{\parindent}{0pt}

\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
\newcolumntype{P}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcommand{\code}[1]{\nolinkurl{#1}}
\newcommand{\OKM}{Open Knowledge Map}
\newcommand{\AINKS}{AI-NKS}
\ifPDFTeX
\newenvironment{prompttranscript}{%
\begin{CJK}{UTF8}{gbsn}%
\begin{quote}%
\small%
\sloppy%
\setlength{\parskip}{0.35em}%
\setlength{\parindent}{0pt}%
}{%
\end{quote}%
\end{CJK}%
}
\else
\newenvironment{prompttranscript}{%
\begin{quote}%
\small%
\sloppy%
\setlength{\parskip}{0.35em}%
\setlength{\parindent}{0pt}%
}{%
\end{quote}%
}
\fi
\lstdefinestyle{okmstyle}{
  basicstyle=\ttfamily\small,
  columns=fullflexible,
  breaklines=true,
  frame=single,
  framerule=0.2pt,
  rulecolor=\color{black!25},
  backgroundcolor=\color{black!2},
  xleftmargin=0.5em,
  xrightmargin=0.5em
}
\lstset{style=okmstyle}
\setlength{\droptitle}{-0.75in}

\title{
  \bfseries\LARGE Open Knowledge Map:\\
  An AI-Native Knowledge Object Infrastructure for\\
  Evidence-Grounded Learning Systems
}

\author{
  \begin{tabular}{c}
    Wenbo Wu$^{1}$ \quad and \quad Aimin Zhou$^{2}$\\[0.35em]
    \small $^{1}$School of Computer Science and Technology, East China Normal University\\
    \small $^{2}$Shanghai Institute of AI for Education\\[0.35em]
    \small $^{1}$\href{mailto:51285900030@mail.ecnu.edu.cn}{51285900030@mail.ecnu.edu.cn}
    \qquad
    $^{2}$\href{mailto:amzhou@cs.ecnu.edu.cn}{amzhou@cs.ecnu.edu.cn}
  \end{tabular}
}

\date{July 13, 2026}

\begin{document}
\maketitle
\vspace{-0.25em}

\begin{abstract}
Large language models and agent systems increasingly operate over textbooks, courses, and other learning materials, but document-chunk retrieval is a weak substrate for repeated educational use. Textbook pages and markdown fragments are searchable, yet they do not by themselves provide stable knowledge identities, typed relations, pedagogical projections, evidence links, quality gates, or a runtime contract that a tutor, planner, or evaluation system can safely call.

This report presents \OKM, an AI-native knowledge object infrastructure for evidence-grounded learning systems. The central claim is that textbooks and disciplinary materials should be treated as source evidence for governable Knowledge Objects rather than as the final computational form of knowledge. \OKM{} organizes extracted objects into a typed Knowledge Network, assembles them into consumption-side Knowledge Units, and keeps generated explanations, source fragments, evidence, media, and graph identities separate.

The current implementation provides a TypeScript-first extraction pipeline, a PostgreSQL-backed knowledge store, a structured unit API, and a React viewer and governance workbench. It supports textbook parsing, lesson-level extraction, staging, transactional merge and normalization, evidence preservation, knowledge-body generation, evidence-linked curriculum projections and pedagogical profiles with pending review, explicitly configured embedding generation, image review, governed multidisciplinary candidate discovery, and layered quality checks. The \code{ai-nks-v0.2} model gives every Knowledge Object one canonical identity, separates disciplinary semantics from curriculum placement, represents cross-domain bridges as ordinary Knowledge Objects, and governs textbooks, papers, encyclopedias, curriculum standards, databases, and expert notes through explicit source policies. Cross-domain scans propose identity alignments, direct relations, or two-segment bridge paths without mutating canonical knowledge; every proposed relation segment requires eligible human-selected evidence before a dataset-locked reducer can apply it. An early Knowledge Runtime always supports lexical retrieval of complete \code{ApiUnit} objects; vector and hybrid execution use stored vectors when a query-embedding service is configured, with execution mode reported in the response. Synchronous and streaming endpoints perform evidence-identifier-validated generation and expose explicit insufficient-context outcomes. A documented June 2026 physics-textbook run produced 288 nodes, 153 edges, 436 evidence rows, and 288 node cards, while also exposing graph-connectivity and extraction-hardening gaps. The report describes the theory, architecture, data model, pipeline, runtime contracts, verification strategy, and remaining work; AI tutoring, semantic planning, learning-outcome evaluation, broad benchmark validation, and the version-governance loop remain future work.
\end{abstract}

\vspace{0.55em}
\noindent{\footnotesize $^\dagger$ Repository working tree inspected on July 13, 2026. This working report intentionally avoids naming its own commit, because committing the report changes that identifier. An archival distribution must bind the report, implementation, public artifact, and verification record to one immutable release tag or commit. The early retrieval and grounded-generation runtime and governed interdisciplinary workflow are implemented, while semantic planning, AI Tutor behavior, learning-outcome studies, version governance, and large-scale adjudicated benchmarks remain future work unless explicitly marked otherwise.}

\newpage

\section{Introduction}

Textbooks are dense, structured, and pedagogically curated, but their computational form is usually weak. A PDF or a markdown conversion preserves surface content, page order, figures, and headings; it does not automatically create durable knowledge identities, distinguish claims from evidence, expose prerequisite relations, or explain which generated statement came from which source. This becomes a practical bottleneck when the goal is to build AI systems that do more than search for passages. Recent work on large language models and LLM-based agents shows that AI systems increasingly generate, plan, use tools, and act over external information, which raises the need for stable and inspectable knowledge substrates \cite{zhao-llm-survey,xi-agent-survey}. A grounded tutor, curriculum planner, graph browser, or assessment generator needs knowledge objects that can be identified, linked, inspected, updated, and called from software.

\OKM{} addresses this bottleneck as an AI-native knowledge object infrastructure rather than as a document-retrieval pipeline. Its current implementation realizes this infrastructure through an extraction pipeline, a governed PostgreSQL-backed knowledge store, structured serving APIs, and a viewer/debug workbench. Three design decisions shape the system. First, the repository treats textbooks as source resources rather than as the final unit of knowledge. A PDF is parsed by MinerU, aligned to an outline, split by lesson or chunk, and processed into staged knowledge candidates. Second, the system separates production-time extraction from consumption-time knowledge views. Model workers create staged rows. After staging quality passes, reducers and normalization write canonical state transactionally; strict QA and graph-integrity checks run afterward. The server later assembles complete \code{ApiUnit} views for users and downstream systems. Third, the project places evidence and governance in the main data model rather than in an optional sidecar. Nodes, relations, domain profiles, cards, and bodies are all expected to remain traceable to mentions, evidence, or source fragments.

The report is organized around three engineering questions. First, what should be the stable computational unit when textbooks are used by AI systems: a document chunk, a chapter heading, or a governed Knowledge Object? Second, where should the system boundary be drawn among source evidence, model-generated candidates, canonical graph state, generated explanatory bodies, and consumption-side units? Third, what minimum pipeline, database, API, runtime, and review contracts are needed before tutoring and planning layers can safely depend on the knowledge store?

This report follows the style of a system technical report. It explains the theoretical foundation, architecture, implemented modules, data boundaries, operational workflow, and research implications of the current repository. The emphasis is on what is already encoded in TypeScript, PostgreSQL, schema files, and viewer behavior, with future work called out explicitly.

\subsection{Contributions}

This report makes six contributions:

\begin{enumerate}
  \item It formulates an evidence-grounded, multidisciplinary Knowledge Object model for learning and research systems, separating the \code{ai-nks-v0.2} conceptual standard from the executable \code{world-v1.3} schema used by the current implementation.
  \item It presents a stage-gated extraction architecture that converts PDF or MinerU markdown into staged lesson/chunk candidates, canonical Knowledge Objects, typed relations, evidence records, semantic Domain Profiles, Curriculum Projections, normalized cards, persistent knowledge bodies, and node- and unit-level embeddings.
  \item It implements a PostgreSQL-centered knowledge store that separates source artifacts, textbook outlines, MinerU status, staging rows, canonical graph tables, evidence, embeddings, pipeline jobs, review decisions, and retrieval candidates while preserving transactional reducer boundaries.
  \item It defines \code{ApiUnit} as the public consumption contract for viewers and runtime components, combining identity, Chinese-labeled relations, semantic Domain Profiles, Curriculum Projections, mentions, evidence, media, source fragments, structured cards, persistent bodies, and completeness signals in one response.
  \item It realizes an early executable runtime for lexical object retrieval, configured vector and hybrid execution, and citation-identifier-validated grounded generation through synchronous and server-sent-event interfaces.
  \item It adds a governed multidisciplinary workflow that separates deterministic candidate discovery from canonical truth, supports explicit bridge-object paths, requires human-selected eligible evidence for every relation segment, and applies approved identity merges and relations through a dataset-locked transactional reducer.
\end{enumerate}

We further document an operational physics-textbook run to show the current system capacity and to identify remaining graph-quality, benchmark, and governance gaps.

\section{Theoretical Foundation}

\subsection{Problem Setting}

\OKM{} starts from a historical problem rather than from a parsing technique. The claim is not that disciplines, curricula, textbooks, or examinations have become useless. The traditional ``discipline--course--textbook--knowledge point'' system solved real problems: it compressed growing knowledge into teachable domains, reduced cognitive load for learners, supported professional specialization, and made large-scale schooling and evaluation administratively possible. Disciplines remain important because they provide methods, language, evidence standards, and learning sequences \cite{sweller,anderson-krathwohl,merrill}.

The structural change introduced by AI is that knowledge is no longer used only by human readers, teachers, and test takers. It is also retrieved, interpreted, combined, checked, generated, and called by AI systems \cite{zhao-llm-survey,xi-agent-survey}. Under these conditions, the conventional textbook ``knowledge point'' becomes too ambiguous as the basic computational unit. A chapter paragraph or document chunk may mix definitions, examples, diagrams, exercises, analogies, and teaching cues in the same span. It often leaves source status, object boundary, prerequisite structure, applicability conditions, and evidence strength implicit.

The theoretical problem for \OKM{} is therefore not how to cut textbooks into smaller pieces, nor how to draw a denser graph. The problem is how to reconstruct the traditional educational arrangement into a human-AI collaborative Knowledge Object network that is computable, traceable, teachable, callable at runtime, and able to evolve. Stated more directly, after AI becomes a participant in knowledge retrieval, explanation, reasoning, generation, and execution, the basic question is how to transform the traditional ``discipline--course--textbook--knowledge point'' system into an infrastructure in which humans and AI can share, inspect, verify, and operate over the same knowledge objects.

\subsection{Why Traditional Knowledge Systems Worked}

Before proposing a new knowledge system, it is necessary to state why the older system worked. At the epistemic level, disciplines divide a complex world into relatively stable objects, methods, terms, and evidence norms. Physics, biology, history, mathematics, and other domains are not arbitrary labels; they define what counts as a legitimate question, explanation, proof, or observation within a domain \cite{bernstein-classification}.

At the learning level, disciplines and courses reduce cognitive burden. Learners cannot process every possible relation across the world at once. A curriculum narrows the field, orders concepts, and gives learners a sequence in which schemas can form. A textbook then gives the sequence a durable expression through definitions, examples, figures, exercises, and chapter order \cite{sweller,anderson-krathwohl,merrill}.

At the institutional level, the same structure makes mass education possible. Stable subjects, standard textbooks, teacher qualifications, lesson hours, examinations, and grade boundaries give schools a manageable way to organize teaching and assessment. A curriculum standard is therefore not just a knowledge list. It is also a governance device that decides what enters school, how deeply it is taught, and how achievement can be evaluated \cite{soysal-strang-mass-education,bernstein-classification}.

\OKM{} keeps this historical value. It does not reject disciplines. It changes their role. In an AI-native knowledge system, disciplines become domain perspectives, explanation frames, evidence norms, learning-path constraints, and application contexts rather than the only possible structure for organizing knowledge. The claim is therefore not that subject boundaries should disappear. The claim is that subject boundaries should no longer be the only unit through which knowledge is identified, reused, retrieved, and governed when AI systems participate in learning and reasoning.

\subsection{Knowledge Systems as Historical Arrangements}

The discipline--course--textbook--examination system is not a natural or permanent form of knowledge. It is a historical arrangement that became effective under particular social, technical, and institutional conditions. A useful way to frame \OKM{} is therefore comparative: different societies have organized knowledge around different units, and each arrangement reflected the problems that society needed knowledge to solve.

The Chinese classical knowledge order was not organized around modern school subjects such as physics, chemistry, biology, economics, or political science. Its mainstream institutional form was centered on classics, moral cultivation, political order, textual interpretation, literary competence, and official selection. Late imperial civil examinations made Confucian textual learning a durable governance and selection mechanism, binding knowledge, status, state service, and cultural legitimacy together \cite{elman-civil-examinations}. This example matters because it shows that a knowledge system can be organized around classics and statecraft rather than around modern disciplinary objects and methods.

The modern Western school and university order took a different path. Early modern didactic thought, including Comenius's universal teaching ideal, treated instruction as a systematic problem of how to organize and transmit knowledge \cite{comenius-great-didactic}. In the nineteenth century, mass schooling became tied to state formation, compulsory education, public administration, and standardized institutional expansion \cite{soysal-strang-mass-education}. At the higher-education level, the Prussian reform context associated with Humboldt and the Berlin institutions helped articulate the research-university ideal, where knowledge was not merely fixed material for transmission but an open problem pursued through scholarly inquiry \cite{humboldt-higher-institutions}. In curriculum sociology, Bernstein later made explicit that the selection, classification, distribution, transmission, and evaluation of public educational knowledge reflect social organization and power, not only neutral epistemic order \cite{bernstein-classification}.

This historical comparison supports the central claim of \OKM{}. The older discipline--course--textbook--knowledge-point structure remains valuable, but it is not the final possible form of knowledge organization. If imperial China could organize knowledge around classics, moral order, and examinations, and modern industrial society could organize knowledge around subjects, textbooks, credentials, and professions, then AI-mediated society can require another arrangement. The proposed arrangement is not anti-disciplinary. It treats disciplines as important perspectives and constraints, while moving the basic managed unit toward Knowledge Objects that can be shared by human learning, AI retrieval, grounded generation, tutoring, assessment, and governance.

\subsection{Why AI Changes the Unit of Knowledge}

AI changes the operating conditions of knowledge systems in four ways. First, knowledge access is less often the primary bottleneck. Learners and teachers can ask for explanations, summaries, comparisons, and examples on demand. Second, the cost of cross-domain connection is lower. A problem can call physics, chemistry, mathematics, engineering, and social context in the same reasoning process. Third, knowledge expression can be generated dynamically. The same underlying knowledge may appear as a textbook explanation, teacher note, learner answer, exercise, diagram, simulation prompt, or project task. Fourth, knowledge use becomes operational. AI systems may not only display knowledge; they may select it, compose it, check it, use it to plan learning paths, diagnose misunderstanding, or call tools.

These changes do not remove the need for structure. They make structure more important. AI systems can generate fluent text without stable object boundaries, combine domains without sufficient evidence, and personalize explanations while drifting away from curriculum goals. The hallucination problem in natural-language generation and the growing interest in unifying LLMs with knowledge graphs both point to the same requirement: generated text needs external structure, provenance, and checkable factual grounding \cite{ji-hallucination,pan-llm-kg}. A system that only stores text chunks cannot reliably decide which span is a definition, which span is an example, which claim is evidenced, which relation is pedagogically necessary, and which condition limits applicability.

Education policy language is also moving in this direction. UNESCO's 2024 student AI competency framework frames students not only as users of AI tools, but as responsible and creative participants who need competencies across human-centered mindset, AI ethics, AI techniques and applications, and AI system design \cite{unesco-ai-students}. Its teacher framework similarly treats the teacher role as changing in a teacher--AI--student relationship, with competencies across human-centered mindset, AI ethics, AI foundations and applications, AI pedagogy, and professional learning \cite{unesco-ai-teachers}. These frameworks do not by themselves define a knowledge schema, but they show that AI education is shifting from fixed content coverage toward responsible judgment, tool use, design, pedagogy, and human agency in AI-mediated environments.

The conclusion is that the AI era does not simply need more text. It needs knowledge infrastructure. Knowledge must remain understandable to humans, but it must also become stable enough for machines to retrieve, join, constrain, verify, and call. This is why \OKM{} moves the basic unit from chapter, lesson, knowledge point, exam label, or chunk toward a governed Knowledge Object.

\subsection{From Knowledge Point to Knowledge Object}

A Knowledge Object is the proposed basic unit of the new system. It is not a textbook heading, a curriculum-standard item, an examination point, or a retrieval chunk. It is a knowledge identity that can be uniquely addressed, defined, connected to other objects, grounded in evidence, exposed through software contracts, and governed over time. This proposal is related to earlier learning-object work, but it is stricter: the object is not only reusable instructional content, but also a governed identity with evidence, relations, pedagogical projections, and runtime semantics \cite{wiley-learning-objects,koedinger-kli}.

This target model gives \OKM{} six structural expectations:

\begin{enumerate}
  \item Identity: stable id, name, aliases, domain, kind, status, and version boundary.
  \item Semantic core: definition, core claims, formulas, conditions, scope, counterexamples, and misconceptions when evidence supports them.
  \item Relations: prerequisite, taxonomic, structural, causal, representational, application, analogy, and weak association links.
  \item Pedagogy: learning objectives, difficulty, teaching roles, diagnostic value, remediation cues, and likely misconceptions.
  \item Computation: retrievable summaries, typed relations, applicability constraints, generation constraints, and stable service contracts.
  \item Governance: source evidence, review status, confidence, update history, risk markers, and feedback paths.
\end{enumerate}

The current repository implements the substrate-level subset of this target model. It supports stable object identity, controlled types, semantic-core fields, typed relations, evidence grounding, Domain Schemas, semantic Domain Profiles, separate Curriculum Projections, source policies, structured cards, persistent bodies, source fragments, unit-completeness signals, governed direct-relation and bridge-path candidates, and pipeline quality gates. Full lifecycle governance and runtime pedagogical adaptation remain future work.

This definition also clarifies what a Knowledge Object is not. It is not admitted merely because a word appears in a textbook. It is not identical to a chapter heading, curriculum bullet, exam point, or model-generated phrase. In the current implementation, a candidate becomes useful only when its identity, source evidence, relation potential, domain profile, structured card, and quality state are sufficient for downstream inspection or use.

\subsection{Repositioning Disciplines, Curricula, Textbooks, and Exams}

The new system does not cancel older educational structures. It gives each of them a clearer role in the Knowledge Object network:

\begin{table}[H]
\centering
\small
\begin{tabularx}{\textwidth}{@{}lYY@{}}
\toprule
\textbf{Source} & \textbf{Role in \OKM} & \textbf{Boundary}\\
\midrule
Discipline & Domain perspective, explanation frame, evidence norm, and learning-path condition. & It is not the only organizing structure.\\
Curriculum standard & Learning range, goals, competency requirements, and stage boundary. & It is not directly a node list.\\
Textbook & Evidence source, expression form, examples, order, diagrams, tables, and formulas. & It does not directly determine object boundaries.\\
Textbook catalog & Splitting, navigation, and sequencing cue. & It is not itself a knowledge graph.\\
Exam point or question bank & Task type, evaluation weight, common error, and mastery signal. & It does not decide whether knowledge exists.\\
Teacher expert & Review, boundary judgment, misconception confirmation, and quality governance. & It should not manually maintain every low-value candidate.\\
Learner data & Misconception, gap, remediation, and adaptation signal. & It should not directly rewrite factual knowledge.\\
\bottomrule
\end{tabularx}
\caption{Theoretical role of traditional educational sources in the Knowledge Object network.}
\end{table}

This repositioning is important for engineering. If every curriculum item, catalog line, or examination label is treated as a node by default, the graph becomes a copy of existing administrative structure. If textbook evidence is ignored, the graph becomes ungrounded generated content. \OKM{} instead uses these sources as constraints over Knowledge Object admission, naming, granularity, evidence, and teaching use.

\subsection{Design Principles for an AI-Native Knowledge System}

The theoretical foundation can be summarized as eight design principles:

\begin{enumerate}
  \item Addressable: knowledge must have stable identities across books, courses, systems, and time.
  \item Definable: a unit should expose a clear semantic core, not only a fuzzy text span.
  \item Evidential: important claims, relations, formulas, examples, and explanations should be traceable to sources.
  \item Relational: objects should enter a network of prerequisite, composition, causal, representational, application, and analogy relations.
  \item Pedagogical: objects should support learning goals, diagnosis, misconception detection, remediation, and evaluation.
  \item Computable: objects should be retrievable, composable, constrainable, and callable by AI systems.
  \item Expressive: the same object should support multiple realizations, such as textbook explanation, teacher material, learner question answering, exercise, diagram, or simulation.
  \item Governable: objects need status, review, version, risk, feedback, and evolution mechanisms.
\end{enumerate}

These principles explain why the repository invests in identity, evidence, staging, QA, and service contracts before building a polished AI Tutor. The tutor is a future application; the current work is to make the underlying knowledge substrate trustworthy enough to call.

\subsection{Implemented Boundary in This Repository}

The current repository implements an executable subset of this theory. It does not yet implement the whole AI-native knowledge system. The conceptual layer describes the target system: Knowledge Objects, Knowledge Units, multidisciplinary Knowledge Networks, Knowledge Runtime, and governance. The executable layer is the current \code{world-v1.3} schema and service contract.

In the implementation, \code{world_nodes} stores the identity skeleton and minimal semantic fields for Knowledge Objects. Richer content lives across \code{properties.semantic_core}, \code{world_edges}, \code{world_evidence}, \code{world_mentions}, semantic \code{world_domain_profiles}, separate \code{world_curriculum_projections}, cards, persistent bodies, media, and source fragments. The public consumption boundary is therefore \code{ApiUnit}, not a node row alone.

This boundary matters for the rest of the report. \OKM{} currently demonstrates the knowledge substrate and an early runtime subset: staged extraction, canonical merge, typed relations, evidence links, cards, bodies, pending-review curriculum-scoped pedagogical profiles, node and unit embeddings, unit completeness, object-level retrieval, citation-identifier validation, governed cross-domain candidate review and application, QA, graph-integrity checks, and viewer/API inspection. Semantic planning, runtime tutoring, learner-feedback write-back, mature multi-expert governance, version evolution, and broad benchmark evaluation remain future work.

\section{System Positioning and Boundary}

\subsection{From Document Retrieval to Knowledge Infrastructure}

A common way to use AI over textbooks is to chunk the source document, embed the chunks, retrieve relevant text, and ask a model to answer from the retrieved passages. This follows the broad retrieval-augmented generation pattern \cite{rag,gao-rag-survey}, but it has structural limits. The chunk is not a stable knowledge object; it may mix definitions, examples, figures, exercises, and narrative text. Relations among concepts remain implicit. If a generated explanation is wrong, it is difficult to decide whether the fault lies in source parsing, retrieval, model interpretation, or missing evidence.

\OKM{} uses the document as input, but not as the final interface. The pipeline extracts explicit objects and relations, attaches evidence, stores structured cards, and later generates durable knowledge bodies. This turns retrieval from ``find a nearby passage'' into ``load a knowledge unit with its evidence and graph neighborhood.''

\subsection{What OKM Is Not}

The current project boundary is deliberately narrower and more precise than several nearby ideas. \OKM{} is not an ordinary textbook graph whose purpose is only to visualize chapter concepts. It is not a RAG chunk library, because chunks are source material rather than governed object identities. It is not a standalone K-Unit teaching package, because the current public contract is \code{ApiUnit}, assembled from the maintained database. It is also not a node network built only for frontend display; the graph, evidence, staging, and unit contracts are intended to serve later retrieval, generation, planning, tutoring, and governance.

\subsection{Conceptual Standard and Executable Schema}

The repository uses two standard layers. The conceptual layer is the top-level language for the AI-native knowledge system. In repository documents this layer is versioned as \code{ai-nks-v0.2}; it frames the project as knowledge infrastructure organized around canonical Knowledge Objects, semantic Domain Profiles, Curriculum Projections, multidisciplinary bridge paths, Knowledge Units, Knowledge Runtime, and governance. The executable engineering layer is the active PostgreSQL and JSON Schema baseline. In repository documents this layer is versioned as \code{world-v1.3}; it defines the actual storage schema, node kinds, Domain Schemas, source policies, relation types, evidence tables, cards, bodies, and QA behavior. This layering is close to the broader knowledge-graph literature, where identity, schema, context, validation, acquisition, and provenance are treated as separate design concerns \cite{hogan-kg,ji-kg-survey,rdf,owl,shacl,prov}.

This distinction matters because it prevents two mistakes. The first is calling the current graph schema the whole research vision. The second is mixing disciplinary meaning, teaching placement, and source authority into one profile. The repository currently treats \code{world-v1.3} as the executable graph substrate under the broader \AINKS{} direction.

\subsection{Terminology Boundaries}

\begin{table}[H]
\centering
\small
\begin{tabularx}{\textwidth}{@{}lYY@{}}
\toprule
\textbf{Term} & \textbf{Meaning} & \textbf{Current engineering status}\\
\midrule
Knowledge Object & Governable knowledge identity with evidence, relations, teaching use, and evolution boundary. & Top-level theoretical core.\\
\code{world_nodes} & Identity skeleton for a Knowledge Object: id, name, kind, definition, status, and minimal semantic data. & Current PostgreSQL table.\\
Knowledge Unit & Consumption-side unit for viewer, retrieval, generation, and later tutoring. & Expressed through \code{ApiUnit}.\\
\code{ApiUnit} & Public view assembled by \code{node_id}: node, Chinese-labeled relations, semantic domain profiles, curriculum projections, mentions, evidence, media, source fragments, card, and body. & Current most important service contract.\\
K-Unit & Earlier idea for a learning or collaboration unit. & Only a conceptual source; not the current schema or API.\\
Knowledge Realization & A generated expression of the same object, such as textbook explanation, exercise, video, simulation, or tutor response. & Future extension.\\
Knowledge Runtime & Mechanism for object retrieval, semantic planning, grounded generation, tutoring, feedback, and evolution. & Early subset implemented for unit retrieval, citation-identifier checks, generation, and streaming; planning, tutoring, feedback, and evolution remain future work.\\
\bottomrule
\end{tabularx}
\caption{Core terminology boundaries used by the current report.}
\end{table}

The immediate engineering rule is that \code{world_nodes} does not equal a complete knowledge point, \code{ApiUnit} is the current public knowledge-unit view, and early K-Unit JSON examples should not be treated as an executable API. This keeps theory, storage, and consumption contracts aligned without making the database schema more complex than the current implementation can justify.

\subsection{Knowledge Units as the Consumption Boundary}

In the current code, a knowledge point is not a single row in \code{world_nodes}. A node row is only the identity core. The complete consumption unit is \code{ApiUnit}, assembled by the server from several tables. This contract is important for future AI use because a tutor or generator should not have to hand-join database tables or guess whether a field is disciplinary meaning, curriculum placement, source text, generated body text, or structured summary. It should request one unit and receive the node, its graph neighborhood, Domain Profiles, Curriculum Projections, evidence, card, body, media, and source fragments in a stable shape. The educational motivation is aligned with work on intelligent tutoring systems, learner modeling, and learning analytics: useful tutoring needs a domain model, a learner model, task selection, feedback, and evidence about changing learner state \cite{vanlehn,corbett-anderson,piech-dkt,bull-kay,koedinger-kli,siemens-learning-analytics}.

\subsection{Implemented and Planned Layers}

\begin{table}[H]
\centering
\small
\begin{tabularx}{\textwidth}{@{}lYY@{}}
\toprule
\textbf{Layer} & \textbf{Implemented or emerging} & \textbf{Planned}\\
\midrule
Source & PDF, MinerU markdown, images, formulas, tables, outlines, and enrich-book context. & Obsidian notes, external knowledge bases, and curated human-authored bodies.\\
Evidence & \code{world_evidence}, \code{world_mentions}, and \code{source_fragments}. & Finer evidence chains and conflict management.\\
Knowledge Object & \code{world_nodes}, nine official kinds, and \code{properties.semantic_core}. & Version evolution and object-level impact analysis.\\
Knowledge Network & One canonical \code{world_edges} table with eighteen active relation types, Chinese display metadata, and governed cross-domain identity, direct-relation, and bridge-path candidates. & Adjudicated evaluation of cross-domain discovery and relation accuracy.\\
Domain and pedagogy & Semantic \code{world_domain_profiles} validated by Domain Schemas, plus separate \code{world_curriculum_projections} with evidence-linked, pending-review pedagogical profiles. & Exam weighting, learner adaptation, and validated pedagogical effectiveness.\\
Knowledge Unit & \code{ApiUnit}, unit API, completeness score, viewer presentation, and bounded generation context. & JSON export and score-driven repair workflow.\\
Runtime & Object-level lexical retrieval; configured vector and hybrid execution with text-only reporting; unit embeddings; citation-identifier validation; insufficient-context handling; synchronous and streaming responses. & Semantic planning, AI Tutor behavior, learner-state adaptation, and feedback write-back.\\
Governance & Staging, merge, normalization, strict QA, graph integrity, quality dashboard, image review, merge review, and interdisciplinary evidence review. & Multi-expert adjudication, version governance, and learning-feedback write-back.\\
\bottomrule
\end{tabularx}
\caption{Current implementation boundary and planned AI-NKS layers.}
\end{table}

\subsection{Relation to Prior Work}

\OKM{} combines ideas from several established lines of work but places the engineering boundary differently. Retrieval-augmented generation grounds model output in external passages, while recent surveys emphasize retrieval quality, generation faithfulness, and evaluation as separate concerns \cite{rag,gao-rag-survey}. Knowledge-graph research contributes stable identity, typed relations, schema, provenance, and validation, and work on combining knowledge graphs with large language models motivates graph-aware retrieval and generation \cite{hogan-kg,ji-kg-survey,pan-llm-kg}. Learning-object, educational-knowledge-graph, and tutoring research contributes reuse, domain modeling, learner modeling, task selection, and pedagogical adaptation \cite{wiley-learning-objects,qu-education-kg,vanlehn,koedinger-kli}.

The distinct contribution of the current system is not a new embedding model or a claim that graphs universally replace document retrieval. It is an executable boundary for evidence-grounded, multidisciplinary knowledge infrastructure: source fragments remain evidence; source authority is governed by policy; model workers write staging rows; reducers control canonical identity; Domain Profiles remain distinct from Curriculum Projections; cards and bodies remain distinguishable from source text; complete \code{ApiUnit} objects become the serving and retrieval boundary; and generated answers are checked against evidence identifiers in the retrieved units. The implemented ingestion path remains textbook-first, while the storage and governance contract is source-general. This creates a testable substrate for later comparison with chunk-based retrieval and educational applications without treating those future results as already established.

\section{System Overview}

Figure~\ref{fig:architecture} shows the implemented construction and serving substrate, including the early runtime subset but not the full future Knowledge Runtime. The production flow starts from a PDF textbook or a previously generated MinerU markdown file. The pipeline creates or loads a textbook outline, splits the material into lessons or chunks, attaches optional enrich-book hints from PostgreSQL, calls model-based extraction, optionally asks a vision model to classify image evidence, writes staged records, applies quality gates, merges and normalizes canonical objects, generates knowledge bodies and curriculum-scoped pedagogical profiles, optionally backfills node and unit embeddings through an operator-configured endpoint, scans multidisciplinary candidates, and runs strict QA, graph integrity, and quality-dashboard stages. Candidate scanning writes governance tables only. Human-approved identity alignments, direct relations, and evidence-backed bridge-path segments are applied separately through a transactional reducer. PostgreSQL is the authoritative structured store. The Hono server reads from it and exposes graph bundles, complete units, text/vector/hybrid unit retrieval, grounded generation, pipeline status, quality diagnostics, image review, multidisciplinary governance, and textbook workbench routes. The React viewer consumes only these API contracts.

\begin{figure}[H]
\centering
\resizebox{0.95\textwidth}{!}{%
\begin{tikzpicture}[
  node distance=0.55cm and 0.7cm,
  box/.style={draw, rounded corners=2pt, align=center, minimum height=0.8cm, text width=2.6cm, fill=blue!4},
  store/.style={draw, cylinder, shape border rotate=90, aspect=0.25, align=center, minimum height=1.0cm, text width=2.4cm, fill=green!5},
  arr/.style={-{Latex[length=2mm]}, thick}
]
  \node[box] (pdf) {PDF textbook\\or URL};
  \node[box, right=of pdf] (mineru) {MinerU\\markdown};
  \node[box, right=of mineru] (outline) {Outline\\and chunks};
  \node[box, right=of outline] (enrich) {Enrich books\\context hints};
  \node[box, below=of outline] (extract) {Lesson/chunk\\model workers};
  \node[box, left=of extract] (vlm) {Vision model\\image review hints};
  \node[store, below=of extract] (pg) {PostgreSQL\\knowledge store\\and vectors};
  \node[box, right=of extract] (qa) {Merge\\normalize\\QA};
  \node[box, right=of qa] (bodies) {Knowledge-body and\\curriculum-profile writers};
  \node[box, below=of pg] (api) {Hono API\\unit retrieval\\and generation};
  \node[box, right=of api] (viewer) {React viewer\\governance workbench};

  \draw[arr] (pdf) -- (mineru);
  \draw[arr] (mineru) -- (outline);
  \draw[arr] (outline) -- (extract);
  \draw[arr] (enrich) -- (extract);
  \draw[arr] (vlm) -- (extract);
  \draw[arr] (extract) -- (qa);
  \draw[arr] (qa) -- (bodies);
  \draw[arr] (extract) -- (pg);
  \draw[arr] (qa) -- (pg);
  \draw[arr] (bodies) |- (pg);
  \draw[arr] (pg) -- (api);
  \draw[arr] (api) -- (viewer);
\end{tikzpicture}
}%
\caption{Current \OKM{} architecture. Structured state is centered in PostgreSQL; generated files remain local artifacts and are not treated as the canonical index. Enrich-book content is auxiliary context, and interdisciplinary discovery remains outside canonical truth until human review and reducer application.}
\label{fig:architecture}
\end{figure}

\subsection{Repository Layout}

\begin{table}[H]
\centering
\small
\begin{tabularx}{\textwidth}{@{}lY@{}}
\toprule
\textbf{Path} & \textbf{Responsibility}\\
\midrule
\code{packages/types} & Shared TypeScript models and API types, including graph models, pipeline responses, outline structures, and the \code{ApiUnit} contract.\\
\code{packages/pipeline} & Extraction pipeline, MinerU integration, outline parsing, enrich-context loading, lesson extraction, staging, transactional merge and normalization, retrieval candidates, body, pedagogical-profile, embedding generation, interdisciplinary candidate discovery and application, QA, and progress tracking.\\
\code{packages/server} & Hono API server, PostgreSQL query layer, graph and unit assembly, complete-unit retrieval, grounded generation and streaming, pipeline launch/status, interdisciplinary governance, review, and textbook routes.\\
\code{packages/viewer} & React/Vite graph viewer, G6 graph canvas, unit detail and grounded-answer panels, interdisciplinary workbench, textbook tree, pipeline debug page, image review, annotation, and frontend API client.\\
\code{schemas} & World knowledge standard, JSON Schemas, architecture notes, extraction templates, and PostgreSQL schema files.\\
\code{docs} & Theoretical foundation, theory decision record, current architecture, AI-NKS standard, interdisciplinary governance contract, knowledge unit contract, prompt inventory, reports, and run notes.\\
\bottomrule
\end{tabularx}
\caption{Main repository modules.}
\end{table}

\section{Knowledge Standard and Data Model}

\subsection{Top-Level Node Kinds}

The executable schema fixes nine top-level node kinds. The design draws on upper-ontology practice, semantic-web standards, and controlled vocabulary principles: object type, taxonomy, relation, and evidence are separated rather than collapsed into one tag field \cite{bfo,owl,skos,hogan-kg}.

\begin{center}
\small
\begin{tabular}{@{}lll@{}}
\toprule
\textbf{Kind} & \textbf{Role} & \textbf{Typical examples}\\
\midrule
\code{entity} & Stable referent or object & oxygen, cell, earth, microscope\\
\code{concept} & Abstract meaning unit & inertia, fraction, democracy\\
\code{property} & Measurable or descriptive feature & density, speed, temperature\\
\code{process} & Ongoing process or mechanism & evaporation, photosynthesis\\
\code{event} & Bounded occurrence & historical event, experiment observation\\
\code{method} & Reusable procedure & filtration, equation solving\\
\code{rule} & Law, rule, theorem, principle & Newton's first law\\
\code{representation} & Symbolic or visual representation & equation, map, circuit diagram\\
\code{resource} & Knowledge-bearing medium & textbook, paper, course, video\\
\bottomrule
\end{tabular}
\end{center}

The goal is not to describe every school subject with one flat tag list. Instead, \OKM{} separates object identity, taxonomy, factual relations, domain-specific semantics, curriculum projection, and evidence. Each discipline selects its own legal roles through a versioned Domain Schema; a mathematical theorem, physical experiment, historical event, or computational algorithm need not be forced into one universal role taxonomy.

The current theory boundary also rejects a premature expansion into many new top-level education object types. \code{world_nodes.kind} remains the official top-level type field. More detailed semantic structure is placed in \code{world_nodes.properties.semantic_core}; educational roles such as skill, task, or misconception may become optional pedagogical metadata later, but they are not new top-level node kinds in the current executable schema.

\subsection{Relation Set}

The current relation set contains eighteen active relation types. Stable snake-case codes are used for storage and APIs; Chinese names and sentence templates are used in prompts, review controls, graph details, and generated relation summaries:

\begin{center}
\small
\begin{tabularx}{\textwidth}{@{}lY@{}}
\toprule
\textbf{Relation family} & \textbf{Chinese name (internal code)}\\
\midrule
Classification & 是一种（\code{is_a}），是实例（\code{instance_of}）\\
Structure & 是组成部分（\code{part_of}），包含（\code{contains}）\\
Mechanism and learning & 具有属性（\code{has_property}），产生（\code{produces}），依赖（\code{depends_on}），是前置知识（\code{prerequisite_for}），导致（\code{causes}），影响（\code{affects}）\\
Representation & 表示（\code{represents}），形式化表达（\code{formalizes}），建模描述（\code{models}）\\
Application and analogy & 使用（\code{uses}），应用于（\code{applies_to}），类似于（\code{analogous_to}）\\
Association & 主题是（\code{about}），相关（\code{related_to}）\\
\bottomrule
\end{tabularx}
\end{center}

The controlled set keeps extraction and QA tractable while adding the minimum relations needed for multidisciplinary explanation: formalization, application, analogy, and modeling. New relations must enter through explicit schema extension rather than by overloading tags. The retired \code{same_as} code is migration-only: confirmed identity equivalence merges nodes instead of creating an edge. This follows the same separation between formal schema and lightweight discovery labels used in semantic-web and knowledge-organization work \cite{rdf,owl,skos,shacl}.

\subsection{Evidence Plane}

Every useful knowledge system needs an answer to the question: why should this object, relation, or explanation be trusted? \OKM{} answers through a cross-cutting evidence plane, following the provenance principle that generated or asserted claims should be traceable to sources, activities, and derivation records \cite{prov}. This is especially important for generation-heavy systems, where hallucination, stale knowledge, and non-transparent reasoning motivate explicit source grounding and audit trails \cite{ji-hallucination,gao-rag-survey}.

\begin{itemize}
  \item \code{world_mentions} records mentions of a knowledge object in a source.
  \item \code{world_evidence} records source-backed evidence units, including text, image, table, formula, and other modalities.
  \item \code{world_node_cards} stores structured summaries that are easier to inspect and use for body generation.
  \item \code{world_node_bodies} stores persistent knowledge bodies for reading and generation.
  \item \code{source_fragments} in \code{ApiUnit} represent textbook source material and should not be confused with generated bodies.
  \item \code{world_enrich_books} stores auxiliary textbook-workbench content and extraction hints; it is not by itself evidence for canonical nodes or relations.
\end{itemize}

\section{Extraction Pipeline}

The extraction pipeline is implemented in \code{packages/pipeline}. Its main command is:

\begin{lstlisting}[language=bash]
npm run server-pipeline-run -w packages/pipeline -- \
  --book-id chem-grade8 \
  --pdf-path /abs/path/to/book.pdf \
  --subject chemistry \
  --school-stage junior-secondary \
  --grade-band grade-8 \
  --skip-embeddings \
  --db "$DATABASE_URL"
\end{lstlisting}

The same command can accept a MinerU file URL, model options, vision-model options, concurrency settings, retry controls, enrich-context controls, node-body options, and pedagogical-profile options. In the current server pipeline path, enrich context is enabled by default with a small limit. The pipeline reads nearby \code{world_enrich_books} material and passes it as \code{lesson_context.enrich_hints}; this context is only allowed to help with terminology boundary, naming, and granularity. It is not evidence for creating nodes or relations. Pipeline embedding backfill requires an explicitly configured \code{EMBEDDING_URL} and uses \code{EMBEDDING_API_KEY} when the selected provider requires authentication. Server-side query embeddings currently require both values; otherwise vector execution is unavailable and hybrid requests report text-only execution. No endpoint is contacted by default, and \code{--skip-embeddings} is the safe reproducible choice when an embedding provider has not been selected.

\subsection{Stage Flow}

Figure~\ref{fig:pipeline-flow} summarizes the stage boundary. Lesson workers write only \code{world_lesson_runs} and \code{world_staging_*}. After staging quality passes, reducer-controlled merge, remapping, and normalization perform canonical writes transactionally. Body, pedagogical-profile, and optional embedding stages are followed by interdisciplinary candidate scanning, strict QA, graph integrity, and the quality dashboard. The scan writes only \code{world_interdisciplinary_runs} and \code{world_interdisciplinary_candidates}; it never applies candidates automatically. Pedagogical-profile, embedding, and candidate-scan stages can be explicitly skipped; embedding stages require an operator-configured endpoint.

\begin{figure}[H]
\centering
\resizebox{!}{0.78\textheight}{%
\begin{tikzpicture}[
  node distance=0.32cm,
  stage/.style={draw, rounded corners=2pt, align=center, text width=3.0cm, minimum height=0.62cm, fill=orange!5},
  table/.style={draw, align=center, text width=3.0cm, minimum height=0.68cm, fill=green!5},
  arr/.style={-{Latex[length=2mm]}, thick}
]
  \node[stage] (s1) {1. Prepare source\\PDF or markdown};
  \node[stage, below=of s1] (s2) {2. Build outline\\lesson/chunk plan};
  \node[stage, below=of s2] (s3) {3. Extract nodes\\and evidence};
  \node[stage, below=of s3] (s4) {4. Extract relations\\and image decisions};
  \node[table, right=1.0cm of s3] (staging) {\code{world_lesson_runs}\\\code{world_staging_*}};
  \node[stage, below=of s4] (s5) {5. Staging quality\\and retry};
  \node[stage, below=of s5] (s6) {6. Merge staged\\lessons};
  \node[stage, below=of s6] (s7) {7. Normalize\\cards and\\evidence};
  \node[table, right=1.0cm of s6] (canonical) {Canonical\\\code{world_*}};
  \node[stage, below=of s7] (s8) {8. Generate\\knowledge bodies};
  \node[stage, below=of s8] (s9) {9. Generate school-stage\\pedagogical profiles};
  \node[stage, below=of s9] (s10) {10. Backfill\\node embeddings};
  \node[stage, below=of s10] (s11) {11. Backfill\\unit embeddings};
  \node[stage, below=of s11] (s12) {12. Interdisciplinary\\candidate scan};
  \node[stage, below=of s12] (s13) {13. Strict QA, graph integrity\\and quality dashboard};

  \draw[arr] (s1) -- (s2);
  \draw[arr] (s2) -- (s3);
  \draw[arr] (s3) -- (s4);
  \draw[arr] (s4) -- (s5);
  \draw[arr] (s5) -- (s6);
  \draw[arr] (s6) -- (s7);
  \draw[arr] (s7) -- (s8);
  \draw[arr] (s8) -- (s9);
  \draw[arr] (s9) -- (s10);
  \draw[arr] (s10) -- (s11);
  \draw[arr] (s11) -- (s12);
  \draw[arr] (s12) -- (s13);
  \draw[arr] (s3.east) -- (staging.west);
  \draw[arr] (s4.east) -- (staging.west);
  \draw[arr] (s6.east) -- (canonical.west);
\end{tikzpicture}
}%
\caption{Pipeline stages and write boundary. Lesson/chunk workers write staging rows; reducers control canonical writes; interdisciplinary scanning writes governance candidates only.}
\label{fig:pipeline-flow}
\end{figure}

\subsection{Source Preparation}

Source preparation supports two input modes. With a local PDF, the pipeline invokes MinerU and writes the resulting markdown and images under \code{data/mineru/<book-id>/}. With a remote PDF URL, MinerU can fetch and parse the source directly. Structured source status is persisted through \code{world_mineru_sources}; local markdown and images remain large content files, not the authoritative structured index.

Outline handling is similarly split. The service and pipeline read structured outline metadata from PostgreSQL through \code{world_textbook_outlines}. File-based outline artifacts can be imported or used as working material, but they are not the long-term metadata source. The extraction path can also read \code{world_enrich_books} as auxiliary context for the same textbook position. These enrich hints improve boundary and naming decisions, but the prompt and pipeline treat the current lesson or chunk as the evidence-bearing source.

\subsection{Model Extraction}

The current repository has five primary model-facing workflows, one retry-specific prompt augmentation, and two general P1 augmentation mechanisms:

\begin{itemize}
  \item P1: lesson/chunk knowledge extraction, split into node/evidence extraction and relation extraction in \code{model-lesson-extraction.ts};
  \item P2: textbook image relevance classification, implemented in \code{image-relevance.ts};
  \item P3: knowledge node body writing, implemented in \code{generate-node-bodies.ts};
  \item P4: evidence-linked, pending-review pedagogical-profile generation for each Curriculum Projection, implemented in \code{generate-pedagogical-profiles.ts};
  \item P5: object-level grounded generation over retrieved \code{ApiUnit} context, implemented in the server runtime;
  \item P6: staging-quality retry repair instructions, generated by the pipeline runner and appended to P1 after blocked lesson/chunk runs.
\end{itemize}

P1 also accepts subject extraction-template blocks from the textbook template directory and an optional command-line \code{--prompt} block. P1 uses a structured input payload containing lesson context, page range, source path, markdown excerpt preview, retrieval candidates, evidence hints, optional \code{lesson_context.enrich_hints}, and markdown lines.

The extraction path uses JSON Schema response constraints in addition to natural-language instructions. The project therefore does not rely on prompt text alone for output structure. The prompt explicitly says that enrich hints can help interpret the lesson topic, but cannot serve as node or relation evidence; if enrich hints conflict with the current slice, the current slice wins. This choice is consistent with graph-validation practice: the model may propose content, but schema and quality gates decide whether the content can enter the maintained graph \cite{shacl,hogan-kg}. Appendix~\ref{app:prompt-inventory} lists the prompt text and dynamic prompt wrappers found in the current repository.

\subsection{Curriculum-Scoped Pedagogical Profiles}

Lesson extraction produces semantic Domain Profiles and separate Curriculum Projections. A Domain Profile records the object's role and discipline-specific properties under a versioned Domain Schema; it does not contain school stage, grade, curriculum role, or pedagogical design. After canonical normalization and knowledge-body generation, the pedagogical-profile stage reads each eligible Curriculum Projection together with its canonical node, card, relations, related nodes, and evidence. The model returns learning objectives, a stage-relative difficulty level, diagnostic questions, common errors, assessment tasks, remediation suggestions, extension suggestions, evidence identifiers, and confidence.

Generated profiles are written to \code{world_curriculum_projections.properties_json.pedagogical_profile}. Each projection already fixes one curriculum, domain, school stage, and optional grade band, so the profile no longer needs a stage-keyed compatibility map. It retains the model name, prompt version, generation time, input fingerprint, review status, confidence, and source references. The generator validates that returned source references belong to the evidence identifiers supplied to the model, but this identifier check is not a semantic-entailment test. Generated entries therefore remain \code{pending}. Existing manual profiles and protected reviewed profiles are not silently overwritten; unchanged generated inputs can be skipped, while an explicit overwrite flag is limited to generated entries. Strict QA checks the projection boundary, output fields, provenance metadata, review state, confidence range, and evidence references. This stage makes pedagogy inspectable and reviewable, but it does not establish that the generated teaching design is effective.

\subsection{Image Evidence}

When configured, the vision-model path classifies images into decisions such as core content, supporting content, decorative image, mismatch, or uncertain. The decision is stored in \code{properties_json.image_relevance}. Decorative and mismatched image evidence can be hidden from the consumption view. Unreviewed uncertain images are preserved with \code{review_status=pending} or an implicit pending state and are shown in the debug review workflow, but ordinary unit views hide them until a reviewer marks them approved. Rejected images keep their evidence record but remain hidden from normal unit details.

This design reflects a practical textbook issue: figures can be central knowledge carriers, but many textbook PDFs also contain icons, decorative graphics, QR codes, publisher marks, and layout artifacts. Treating all images as equally useful evidence makes the graph noisy; deleting uncertain images too aggressively loses important context. The current system keeps uncertainty visible to reviewers without promoting unreviewed images into normal unit detail pages, and reviewer decisions are persisted as approved or rejected status rather than being stored only in the browser.

\subsection{Quality Gates and Retry}

After staging writes, \code{staging-quality} checks whether each lesson/chunk produced sufficient and internally consistent staged output. A lesson may legitimately produce no objects only when it explicitly returns \code{lesson_disposition=no_knowledge}, supplies a non-empty \code{no_knowledge_reason}, and leaves all staged knowledge artifacts empty. An implicit empty result, an invalid disposition, or an \code{extracted} result without nodes and evidence is blocked. The server pipeline can rerun only affected anchors instead of restarting the entire book. The generated retry prompt asks the worker to re-evaluate the current slice under the current response schema, permits an explicit \code{no_knowledge} outcome, and forbids fabricating nodes or relations merely to pass the gate.

Non-blocking admission warnings are persisted in \code{world_lesson_runs} under \code{properties_json.quality_warnings}, together with \code{quality_review_required} and \code{review_node_ids}. Synthetic fallback evidence is marked \code{synthetic=true}, \code{quality_excluded=true}, and \code{review_status=pending}; it remains visible for review but cannot satisfy formal evidence-coverage checks. After merge and normalization, \code{strict-qa} validates schema legality, \code{graph-integrity} checks graph-level issues such as cycles, isolated nodes, disconnected components, and final QA markings, and \code{quality-dashboard} materializes lesson-level and canonical quality metrics and review queues for the debug page.

\subsection{Transactional Canonical Writes}

Staging writes already use a transaction so lesson-run state, deletion of prior staged rows, and insertion of a replacement bundle succeed or roll back together. Canonical merge and normalization add a data-set-scoped PostgreSQL advisory transaction lock through \code{pg_advisory_xact_lock}. Each reducer performs its reads, planning, remapping, and writes within the same transaction and rolls back on failure. This prevents concurrent reducers for the same data set from committing plans derived from stale canonical state while still allowing unrelated data sets to proceed independently.

\subsection{Governed Interdisciplinary Discovery}

The multidisciplinary stage deterministically examines canonical nodes whose domain sets are disjoint. Identity-alignment candidates require the same controlled node kind and are blocked by names, aliases, or semantic keys before the existing node-match score is applied. Direct-relation candidates require shared explicit bridge tags and exclude node pairs that already have a canonical edge or an identity-alignment candidate. Bridge-path candidates require an explicit bridge Knowledge Object: a node with a bridge role, universal scope, or multiple Domain Profiles whose bridge signals connect both endpoints. These signals are candidate-recall mechanisms rather than evidence that a fact is true.

Candidate records remain outside \code{ApiUnit.relations}. A reviewer can reject any candidate kind. Approving a direct relation requires one of the eighteen active relation types, directionality and endpoint order, and at least one selected eligible \code{world_evidence} identifier that belongs to the candidate. Approving a bridge path requires an active relation type and eligible evidence for each of its two segments; evidence attached only to the other segment cannot be borrowed. Evidence must still exist, satisfy its source policy, and be neither synthetic, quality-excluded, pending, nor rejected. The separate \code{interdisciplinary-apply} reducer obtains the same dataset-level advisory lock used by canonical repair, follows deprecated-node chains, rechecks current state and evidence eligibility, merges approved duplicate identities or writes evidence-backed edges to the single canonical \code{world_edges} table, records multidisciplinary review provenance, and rolls back the transaction on failure. Approved identity alignment performs a canonical merge; the retired \code{same_as} relation is never created. The scan itself uses no language model prompt.

\section{PostgreSQL Knowledge Store}

The current repository treats PostgreSQL as the only authoritative structured store. The initialization file is \code{schemas/pg/knowledge_store.sql}. The data model can be read in seven groups.

\begin{table}[H]
\centering
\small
\begin{tabularx}{\textwidth}{@{}lY@{}}
\toprule
\textbf{Group} & \textbf{Tables}\\
\midrule
Dataset and sources & \code{world_datasets}, \code{world_source_artifacts}, \code{world_source_policies}, \code{world_textbook_outlines}, \code{world_enrich_library}, \code{world_enrich_books}, \code{world_mineru_sources}\\
Domain and curriculum models & \code{world_domain_schemas}, \code{world_domain_profiles}, \code{world_curriculum_projections}\\
Canonical knowledge & \code{world_nodes}, \code{world_node_terms}, \code{world_edges}, \code{world_taxonomy_terms}, \code{world_taxonomy_edges}, \code{world_mentions}, \code{world_evidence}, \code{world_evidence_links}, \code{world_node_cards}, \code{world_node_bodies}\\
Retrieval support & node embeddings on \code{world_nodes}, complete-unit embeddings in \code{world_unit_embeddings}, and extraction-context rows in \code{retrieval_candidates}\\
Pipeline status & \code{world_pipeline_jobs}, \code{world_pipeline_job_stages}, \code{world_pipeline_job_events}, \code{world_pipeline_worker_states}\\
Staging and merge & \code{world_lesson_runs}, \code{world_staging_nodes}, \code{world_staging_edges}, \code{world_staging_domain_profiles}, \code{world_staging_curriculum_projections}, \code{world_staging_mentions}, \code{world_staging_evidence}, \code{world_staging_node_cards}, \code{world_merge_runs}, \code{world_canonical_node_map}\\
Interdisciplinary governance & \code{world_interdisciplinary_runs}, \code{world_interdisciplinary_candidates}\\
\bottomrule
\end{tabularx}
\caption{PostgreSQL table groups in the current schema.}
\end{table}

This schema makes several boundaries explicit:

\begin{itemize}
  \item \code{world_nodes} stores the identity core, not a full knowledge unit.
  \item \code{world_domain_profiles} stores disciplinary roles and semantics; \code{world_curriculum_projections} separately stores curriculum, stage, grade, teaching roles, and pedagogical profiles.
  \item \code{world_source_policies} governs whether each source class can support relations and whether explicit review is required; source policy does not replace row-level evidence review.
  \item \code{world_node_terms} stores canonical names, aliases, and tags as normalized lookup terms.
  \item \code{world_node_cards} stores structured summaries, not textbook source text.
  \item \code{world_node_bodies} stores persistent knowledge bodies, not textbook source text.
  \item \code{source_fragments} and \code{world_evidence} are the evidence/source-text paths.
  \item \code{world_enrich_books} can provide extraction context, but it is not promoted to evidence unless a later explicit evidence path is added.
  \item Staging and canonical tables remain separate so model output can be checked, merged, and normalized before becoming part of the graph.
  \item Interdisciplinary candidate tables are governance records rather than canonical relations; only reviewed and applied results enter identity or \code{world_edges} state.
\end{itemize}

The unit-embedding table is keyed by data set and node. Its \code{retrieval_text} is assembled from the public unit's identity, definition, semantic core, card, persistent body, and evidence joined to node mentions rather than from a node name alone. The current embedding query does not exclude text evidence solely because it is marked synthetic or quality-excluded, so downstream evaluation must preserve that distinction. A \code{content_hash} lets the backfill stage skip unchanged units, while \code{embedding_model} and \code{generated_at} retain generation provenance. The current vector column has 1024 dimensions. Keeping unit embeddings separate from the identity row allows retrieval representation to evolve without treating an embedding as part of canonical knowledge identity.

\section{Knowledge Unit API}

The most important consumption-side contract is \code{ApiUnit}. It is served by \code{GET /api/source/:key/unit/:node_id}. It is not a single table row; it is an assembled view.

\begin{lstlisting}
interface ApiUnit {
  node: ApiUnitNode;
  relations: {
    outgoing: ApiUnitRelation[];
    incoming: ApiUnitRelation[];
  };
  domain_profiles: ApiUnitDomainProfile[];
  curriculum_projections: ApiUnitCurriculumProjection[];
  mentions: ApiMention[];
  evidence: ApiEvidence[];
  media: ApiUnitMedia[];
  source_fragments: ApiUnitSourceFragment[];
  card: ApiNodeCard | null;
  body: ApiUnitBody | null;
  completeness: ApiUnitCompleteness;
}

interface ApiUnitCompleteness {
  score: number;
  passed: number;
  total: number;
  signals: ApiUnitCompletenessSignal[];
}
\end{lstlisting}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  node distance=0.45cm and 0.65cm,
  source/.style={draw, rounded corners=2pt, align=center, text width=2.45cm, minimum height=0.7cm, fill=green!5},
  unit/.style={draw, rounded corners=3pt, align=center, text width=3.0cm, minimum height=1.1cm, fill=blue!6},
  arr/.style={-{Latex[length=2mm]}, thick}
]
  \node[source] (nodes) {\code{world_nodes}\\identity};
  \node[source, below=of nodes] (edges) {\code{world_edges}\\relations};
  \node[source, below=of edges] (profiles) {\code{world_domain_profiles}\\disciplinary semantics};
  \node[source, right=1.2cm of nodes] (mentions) {\code{world_mentions}\\mentions};
  \node[source, below=of mentions] (evidence) {\code{world_evidence}\\source evidence};
  \node[source, below=of evidence] (cards) {\code{world_node_cards}\\summary card};
  \node[source, right=1.2cm of evidence] (bodies) {\code{world_node_bodies}\\knowledge body};
  \node[source, below=of cards] (curricula) {\code{world_curriculum_projections}\\teaching view};
  \node[unit, below=1.1cm of bodies] (apiunit) {\code{ApiUnit}\\complete consumption\\view};

  \foreach \x in {nodes,edges,profiles,mentions,evidence,cards,bodies,curricula}
    \draw[arr] (\x) -- (apiunit);
\end{tikzpicture}
\caption{Knowledge unit assembly. The service layer combines identity, graph neighborhood, disciplinary semantics, curriculum projections, evidence, source fragments, cards, and bodies into one public unit.}
\label{fig:unit}
\end{figure}

The server implementation in \code{packages/server/src/db/queries.ts} loads the node, outgoing and incoming edges with Chinese labels, semantic Domain Profiles, Curriculum Projections, mentions, matching evidence, card, body, media derived from reviewed evidence, and source fragments derived from evidence and textbook outlines. It then calls \code{buildApiUnitCompleteness} to score the unit across node definition, semantic core, relations, evidence, source fragments, domain profiles, body source references, structured card, and mentions. Pending uncertain images and rejected images are filtered out of ordinary unit media. The viewer should consume this response instead of reconstructing table joins in the browser.

\section{Server Layer and Pipeline Jobs}
\label{sec:server-runtime}

The server is a Hono application in \code{packages/server}. It exposes health, metadata, graph bundles, unit loading, node cards, object-level retrieval, grounded generation, pipeline status, pipeline quality diagnostics, image review, interdisciplinary governance, annotation, asset serving, and textbook workbench routes.

{\small
\begin{longtable}{@{}P{0.40\textwidth}P{0.56\textwidth}@{}}
\caption{Main API routes.}\label{tab:main-api-routes}\\
\toprule
\textbf{Route} & \textbf{Role}\\
\midrule
\endfirsthead
\toprule
\textbf{Route} & \textbf{Role}\\
\midrule
\endhead
\midrule
\multicolumn{2}{r}{\small Continued on next page}\\
\endfoot
\bottomrule
\endlastfoot
\code{GET /api/health} & Health and database readiness check.\\
\code{GET /api/meta} & Available datasets, active source, and textbook metadata.\\
\code{GET /api/source/:key/bundle} & Graph bundle for viewer initialization.\\
\code{GET /api/source/:key/unit/:node_id} & Complete knowledge unit view.\\
\code{GET /api/source/:key/search} & Lightweight node search used by graph navigation.\\
\code{GET /api/source/:key/units/search} & Complete-unit retrieval requested in \code{text}, \code{vector}, or \code{hybrid} mode. Vector execution requires a configured query-embedding service and stored vectors; each hit contains scores, match reasons, and a full \code{ApiUnit}, while the response reports \code{full} or \code{text_only} execution.\\
\code{POST /api/source/:key/grounded-generate} & Retrieves units and returns an evidence-cited answer with identifier-validated and invalid citations, model-reported unsupported claims, used node ids, and grounding status.\\
\code{POST /api/source/:key/grounded-generate/stream} & Server-sent-event form of grounded generation with \code{retrieval}, \code{answer_delta}, \code{complete}, and \code{error} events.\\
\code{GET /api/source/:key/pipeline} & Pipeline summary, recent runs, merge status, and review items.\\
\code{GET /api/source/:key/pipeline/quality} & Quality dashboard payload with lesson metrics, evidence coverage, isolated nodes, disconnected components, and manual-review counts.\\
\code{POST /api/source/:key/pipeline/infer-textbook} & Infers book title, subject, stage, grade band, MinerU language, outline page defaults, and extraction template from the request and stored outline.\\
\code{POST /api/source/:key/pipeline/start} & Starts a detached server-side pipeline job.\\
\code{GET /api/source/:key/pipeline/jobs/:job_id} & Detailed job status from \code{world_pipeline_*}.\\
\code{GET/POST /api/source/:key/image-reviews} & Reads and writes human image-review decisions.\\
\code{GET /api/source/:key/interdisciplinary} & Domain coverage, bridge objects, domain-pair summaries, identity/direct-relation/bridge-path candidates, evidence excerpts, source policies, and the latest scan.\\
\code{POST /api/source/:key/interdisciplinary/analyze} & Runs candidate discovery without writing canonical nodes or edges.\\
\code{POST /api/source/:key/interdisciplinary/candidates/:candidate_id/review} & Approves or rejects a pending candidate; direct relations require selected evidence, while bridge paths require independently selected evidence for each segment.\\
\code{POST /api/source/:key/interdisciplinary/apply} & Applies approved identity alignments, direct relations, and bridge-path segments through the dataset-locked reducer.\\
\code{GET /api/enrich/books} and \code{GET /api/enrich/book} & Textbook workbench metadata.\\
\end{longtable}
}

Pipeline jobs started from the viewer are detached child processes. The server creates a job id, prepares a log path under \code{runs/server-jobs}, passes \code{--job-id} and \code{--log-path} into the pipeline command, and lets the pipeline write structured progress into PostgreSQL. The command builder also forwards user or inferred values such as dataset id, output root, parallelism, extraction template, quality retry count, model retry count, subject, school stage, grade band, OpenAI-compatible model settings, VLM settings, PDF path, MinerU URL, MinerU language, page ranges, and outline page range. This means the browser can close while the job continues; the UI later reads \code{world_pipeline_jobs}, stage rows, events, and worker states to reconstruct status.

Lexical complete-unit retrieval does not require an embedding provider. Vector execution first embeds the query, prioritizes \code{world_unit_embeddings}, and supplements nodes without unit vectors from \code{world_nodes.embedding}. In hybrid mode, an unavailable vector path leaves lexical results and reports \code{mode=text_only}; a vector-only request has no lexical candidate path, so it returns no vector hits and also reports \code{text_only} when query embedding is unavailable.

The grounded-generation path then retrieves bounded \code{ApiUnit} context and validates every model citation by checking that its node and evidence identifiers occur in the retrieved units. This establishes identifier membership, not semantic entailment between every answer claim and its cited excerpt; \code{unsupported_claims} is also model-reported rather than independently detected. Successful response bodies currently distinguish \code{grounded}, \code{partial}, and \code{insufficient_context} states. Although the shared type reserves \code{model_error}, provider or parsing failures currently leave the successful-response path and are reported as an HTTP error or a server-sent-event \code{error} event. The streaming route sends retrieval results before answer deltas and emits the same validated final response in its \code{complete} event.

\section{Viewer and Debug Workbench}

The viewer is a React/Vite application built around five work areas:

\begin{itemize}
  \item graph browsing with filters, detail panels, complete-unit retrieval, and grounded question answering;
  \item textbook navigation through \code{TextbookTreePage};
  \item extraction inspection and control through \code{PipelineDebugPage};
  \item persisted annotation and review workflows;
  \item interdisciplinary scanning, filtering, evidence review, domain-pair inspection, bridge-object navigation, and approved-candidate application through \code{InterdisciplinaryPage}.
\end{itemize}

The architecture and pipeline figures explain backend structure. The four interface figures below use only the repository-authored Synthetic Home Solar Demo. They illustrate public interface and contract behavior rather than textbook extraction accuracy, pipeline completion, or benchmark performance. Figure~\ref{fig:ui-grounded-answer} shows the pre-retrieval question interface; executable grounded-generation behavior is established by the implementation and tests, not by that screenshot.

The graph page first loads \code{/api/meta} and then \code{/api/source/:key/bundle}. Clicking a node loads \code{/api/source/:key/unit/:node_id} and shows the full knowledge unit. The detail view displays relation, evidence, profile, curriculum-projection, mention, and completeness counters; renders the generated knowledge body with evidence references; separates semantic-core groups, textbook source fragments, structured card sections, Chinese relation sentences, disciplinary semantic profiles, curriculum and teaching projections, evidence, and media; and supports full-screen reading of source fragments. This separation is essential because it keeps the user from mistaking generated explanatory text for the textbook source or curriculum placement for disciplinary meaning.

The multidisciplinary workbench keeps discovery and canonical truth visually separate. Summary cards show domain coverage, explicit bridge objects, strict cross-domain edges, and pending or approved candidates. Review controls show Chinese relation names, require explicit evidence selection before direct-relation approval, and require separate relation and evidence choices for both bridge-path segments. Applying approved candidates requires confirmation because identity alignment can merge canonical records. The versioned public artifact remains read-only and does not expose these database-backed governance operations.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{assets/report/graph-overview.png}
\caption{Graph view of the repository-authored Synthetic Home Solar Demo. The directly seeded data set contains nine canonical Knowledge Objects spanning the nine \code{world-v1.3} node kinds and twelve typed relations. The figure demonstrates viewer and graph-contract behavior; it is not an extraction result or evaluation benchmark.}
\label{fig:ui-graph-overview}
\end{figure}

Figure~\ref{fig:ui-unit-detail} shows the synthetic source tree and the selected ``1.2 Solar panel'' chunk. The right panel links that chunk to a directly seeded Knowledge Object and a synthetic evidence excerpt, demonstrating the source-to-object inspection path without third-party textbook content.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{assets/report/unit-detail.png}
\caption{Textbook workbench view of the repository-authored synthetic module. The selected ``1.2 Solar panel'' chunk is linked to a directly seeded Knowledge Object and its synthetic evidence excerpt. These records were not produced by model extraction or reducer execution.}
\label{fig:ui-unit-detail}
\end{figure}

The graph workspace also exposes the early runtime entry point. Figure~\ref{fig:ui-grounded-answer} shows a synthetic solar-panel question entered with hybrid retrieval selected, before retrieval or generation has run. It therefore demonstrates the controls but not a retrieval hit, answer, or citation-validation result.

\begin{figure}[H]
\centering
\includegraphics[width=0.96\textwidth]{assets/report/grounded-answer.png}
\caption{Grounded-question interface with a synthetic solar-panel question entered against the demo graph before retrieval begins. This screenshot shows the runtime entry point only; it does not show a retrieved result, generated answer, or validated citation.}
\label{fig:ui-grounded-answer}
\end{figure}

The debug page covers the operational loop: infer textbook metadata, start a pipeline run, watch stage progress, inspect worker state, inspect quality-dashboard metrics, see blocked conditions, review image evidence, and inspect merge-review items. Image review actions are written back through the server so later unit views can hide \code{review_status=rejected} images or show \code{review_status=approved} core, supporting, or manually kept images.

\begin{figure}[H]
\centering
\includegraphics[width=\textwidth]{assets/report/quality-dashboard.png}
\caption{Quality-dashboard card loaded with the direct-seeded synthetic demo. It reports one synthetic lesson row, nine canonical objects, twelve relations, and 0\% formal evidence coverage; the zero value is expected because demo evidence is marked synthetic and quality-excluded. This is not a recorded MinerU, model-extraction, reducer, QA, or independent-review run.}
\label{fig:ui-pipeline-debug}
\end{figure}

\section{Structured Output and Prompt Governance}

\OKM{} uses model calls, but it does not leave the core data shape to free-form generation. The repository pairs prompt instructions with JSON Schema response constraints. In the lesson extraction path, the model receives serialized lesson context, optional enrich hints, and markdown lines; the response must match the world-knowledge lesson bundle schema. The node/evidence stage must explicitly distinguish \code{extracted} from \code{no_knowledge}, so a valid evidence-free lesson is not confused with an accidental empty model response. In the image path, the model must return \code{visual_summary}, \code{keep}, \code{relevance}, \code{reason}, and \code{confidence}; review status is derived and persisted by later code rather than returned by the model. In the node-body path, the response must contain markdown body content and source references.

This pattern gives the project three useful properties:

\begin{enumerate}
  \item The natural-language prompt expresses extraction policy, such as evidence-first extraction and legal node kinds.
  \item The JSON Schema expresses machine-checkable structure.
  \item The pipeline remains responsible for parsing, repair, staging, merge, normalization, and QA.
\end{enumerate}

The prompt inventory also documents a key production choice: lesson/chunk extraction is intentionally local to the current slice. Cross-lesson consolidation belongs to reducer stages and later graph-level logic, not to a worker that sees only one slice.

The same rule applies to enrich hints. They can reduce naming and granularity errors, but they do not widen the worker's evidence boundary. This keeps auxiliary textbook material from silently becoming canonical proof.

\section{Case Study: Physics Textbook Run}
\label{sec:physics-case-study}

A documented run on a high-school physics textbook provides an early system-level check of the pipeline. The input was a PDF textbook parsed through MinerU, with markdown and images cached under \code{data/mineru/physics-hukj-compulsory-3/}. This is a historical run record from June 26, 2026, not a claim about the live database shown in the July screenshots.

\begin{table}[H]
\centering
\small
\begin{tabularx}{0.92\textwidth}{@{}lY@{}}
\toprule
\textbf{Configuration} & \textbf{Recorded value}\\
\midrule
Source and data set & \code{physics-hukj-compulsory-3}; data set \code{main}\\
Processing unit & 19 lesson-scale runs\\
Extraction model & \code{Qwen3.6-27B} through an OpenAI-compatible interface; the language and vision paths used the same configured model\\
Concurrency & Eight lesson workers and eight image-classification workers\\
Successful merge & \code{merge:73b3e9ab80f8}\\
Run date & June 26, 2026\\
\bottomrule
\end{tabularx}
\caption{Recorded configuration of the historical physics-textbook run. Credentials were supplied through environment variables and are not part of the report artifact.}
\label{tab:physics-run-config}
\end{table}

Table~\ref{tab:physics-run} summarizes the final documented database state.

\begin{table}[H]
\centering
\small
\begin{tabularx}{0.88\textwidth}{@{}l r Y@{}}
\toprule
\textbf{Artifact} & \textbf{Count} & \textbf{Interpretation}\\
\midrule
Lesson runs & 19 & The pipeline processed the textbook by lesson-scale units rather than as one global prompt.\\
Canonical nodes & 288 & Extracted knowledge-object identities after staging, merge, and normalization.\\
Canonical edges & 153 & Typed relations accepted into the current graph.\\
Evidence rows & 436 & Source-backed evidence items retained for inspection and grounding.\\
Mentions & 320 & Links from objects back to source occurrences.\\
Node cards & 288 & Structured summaries available for every accepted node in this run.\\
Domain profiles & 291 & Teaching-oriented projections attached to nodes.\\
Node-term links & 1006 & Normalized canonical-name, alias, and tag lookup records for graph organization.\\
\bottomrule
\end{tabularx}
\caption{Documented output of the physics-textbook run. The counts show system operation, not a final benchmark result.}
\label{tab:physics-run}
\end{table}

The run passed staging quality, canonical merge, normalization, strict QA, and graph integrity marking. Graph integrity still reported structural warnings such as isolated nodes and disconnected components. These warnings are important: they show that the pipeline can finish and persist usable objects, but they also show that extraction success is not the same as a dense, pedagogically complete, or experimentally validated knowledge graph.

The same run exposed practical hardening needs that have shaped the current repository:

\begin{itemize}
  \item Chinese textbook table-of-contents lines required stronger outline parsing.
  \item Model responses sometimes wrapped JSON in markdown code fences, requiring reliable JSON extraction.
  \item Empty database reruns needed dataset bootstrap before progress and staging writes.
  \item PostgreSQL JSONB writes needed object/array parameters rather than stringified JSON.
  \item Some chunks returned zero nodes and needed targeted retry rather than whole-book restart.
  \item Image filtering could remove references that still needed text-evidence repair.
\end{itemize}

The important result is not that one run is enough to validate the research program. The important result is that real textbook extraction revealed failure modes, and those failure modes became pipeline hardening tasks: better parsing, stricter JSON repair, retry controls, database progress persistence, and clearer evidence boundaries.

\section{Verification Strategy}

The repository uses a layered verification strategy:

\begin{itemize}
  \item TypeScript checks across workspaces through \code{npm run check}.
  \item Workspace builds through \code{npm run build}.
  \item Pipeline tests through \code{npm test -w packages/pipeline}.
  \item Server and runtime tests through \code{npm test -w packages/server}.
  \item Stage-level quality checks through \code{staging-quality}.
  \item Schema and data checks through \code{strict-qa}.
  \item Graph-level checks through \code{graph-integrity}.
  \item Debug-page quality summaries through \code{quality-dashboard}.
  \item Focused interdisciplinary planner, review-validation, API-contract, and database smoke checks.
\end{itemize}

These checks target different risks. TypeScript catches contract drift across packages. Pipeline tests catch transformation and SQL behavior. Staging quality checks whether model-produced staged output is usable. Strict QA checks schema legality. Graph integrity looks for structural graph problems that might not violate row-level schema rules. The quality dashboard aggregates lesson and canonical metrics such as evidence coverage, isolated-node ratio, disconnected components, pending image reviews, merge-review counts, interdisciplinary-review counts, blocked lessons, and stored quality issues.

\begin{table}[H]
\centering
\small
\begin{tabularx}{\textwidth}{@{}lYY@{}}
\toprule
\textbf{Check} & \textbf{What it supports} & \textbf{What it does not prove}\\
\midrule
TypeScript check and build & Package contracts still compile across pipeline, server, viewer, and shared types. & Semantic correctness of extracted knowledge.\\
Pipeline and server tests & Stage behavior, SQL writes, data-shape handling, runtime parsing, citation-identifier validation, interdisciplinary planning and review validation, streaming helpers, and focused regression cases. & General extraction, answer, or cross-domain accuracy across subjects.\\
Staging quality & Extracted lesson/chunk outputs are internally usable before merge, while explicit, reasoned \code{no_knowledge} outcomes remain valid. & Human-level boundary judgment for every candidate.\\
Strict QA & Canonical rows satisfy schema and controlled-vocabulary constraints. & Pedagogical usefulness or factual completeness.\\
Graph integrity & Structural graph issues such as cycles, isolated nodes, disconnected components, and final QA state are visible. & That the graph is optimal, dense, or ready for tutoring.\\
Quality dashboard & Operational quality metrics, including interdisciplinary pending review, are visible in the server and debug page. & Independent human evaluation, baseline comparison, or learning-outcome evidence.\\
\bottomrule
\end{tabularx}
\caption{Verification scope of the current repository checks.}
\end{table}

The July 13 verification command \code{npm run verify} covers repository-wide type checks, pipeline, server, and viewer tests, and production builds. Exact test counts are deliberately left to the machine-generated command output so that the report does not become stale whenever coverage grows. The Vite build may emit a bundle-size warning for the main viewer chunk; this does not fail the build, but it remains a frontend performance item. The current test design should continue to grow around business-output parity and database correctness. For database paths, useful tests should create temporary rows, verify writes and reads, and clean up after themselves. For extraction behavior, focused fixtures are more valuable than broad snapshots because the model layer itself is probabilistic.

\section{Preliminary Runtime Evaluation and Reproducibility}

The tracked repository contains a small five-question physics fixture under \code{experiments/}, named \code{runtime-apiunit-grounding-small.jsonl}, and a command-line evaluator:

\begin{lstlisting}[language=bash]
npm run evaluate-runtime -w packages/server -- \
  --source main --db "$DATABASE_URL" --retrieval-mode hybrid
\end{lstlisting}

The fixture contains five hand-authored physics questions, each with one expected node identifier. Without \code{--generate}, the command reports retrieval hit rate and recall against those identifiers. With \code{--generate}, it additionally reports expected-term coverage, identifier-level citation validity, invalid-citation count, and the model-reported \code{unsupported_claims} field. The repository does not include the evaluated database snapshot or a machine-readable output for the July 10 local run, so this report does not present those local values as reproducible effectiveness results.

The fixture and evaluator provide a development smoke check rather than a paper-grade benchmark: they cover one subject, only five questions, locally assigned node identifiers, and no independent adjudication. The tracked repository also includes pilot and ablation scripts, manuals, and result summaries. Generated experiment outputs remain excluded by default, and the current materials do not provide a completed independent human-scoring record; this report therefore makes no paper-grade comparative-performance claim from them.

\subsection{Artifact, Data, and Rights Boundary}

This draft describes the repository state inspected on July 13, 2026. The archival report must identify a release commit or tag that contains the implementation, cited artifacts, and verification record together.

The repository retains a read-only historical inspection artifact at \code{artifacts/okm-public-v0.1.0}. Its \code{main} dataset exports 182 Knowledge Objects, 144 typed relations, 537 evidence records, 182 cards, 182 bodies, and 184 stage-scoped pedagogical profiles as JSON, together with a manifest, checksums, JavaScript and Python readers, and a static viewer. This snapshot predates \code{world-v1.3}; it remains immutable and must not be read as the current Domain Profile or Curriculum Projection contract. The current exporter targets a new \code{v0.2.0} artifact, which is not yet tracked. The historical artifact is a preview rather than a cleared data release: the source manifest records bibliographic identity, edition, printing, and ISBN from the local PDF, but the PDF reserves rights and no applicable open license or explicit redistribution permission is recorded. The repository's absent root software license and the artifact's rights notes therefore remain binding release blockers.

Figures~\ref{fig:ui-graph-overview}--\ref{fig:ui-pipeline-debug} use \code{examples/demo-data}, a repository-authored synthetic home-solar module with nine directly seeded Knowledge Objects, twelve typed relations, and nine synthetic evidence records marked \code{quality_excluded}. It is not a real textbook, a model-extraction run, or an evaluation benchmark. A fresh clone can use this module to exercise database, API, and interface contracts without a model provider.

The \code{examples/sample-data} directory is a legacy import-fixture set and is not public-safe as a whole. Its small Markdown snippets are explicitly synthetic, while its named-edition outlines and enrichment trees require file-by-file provenance and redistribution review. Original textbook PDFs, OCR or MinerU derivatives, source images, development database snapshots, and run logs are not public release artifacts. Reproducing the historical physics counts requires authorized access to the source material, compatible configured services, and the recorded run configuration; those counts are operational observations rather than a released benchmark.

\subsection{Ethics, Authorship, and Disclosure Boundary}

This technical report evaluates software and textbook-derived artifacts; it contains no learner study, human-participant experiment, or personal learner data. The four public interface figures use only the repository-authored synthetic demo and contain no third-party textbook passage or real individual record. The historical physics case study reports aggregate engineering observations; its source PDF, extracted passages, images, and database are not redistributed. Any future classroom deployment, learner-state modeling, or feedback write-back requires an appropriate consent, privacy, and institutional-review process.

Running the extraction pipeline may transmit source material or derived text to configured MinerU, language-model, vision-model, or embedding providers. Operators must confirm processing rights, provider terms, retention policies, confidentiality requirements, and any necessary consent before using protected or personal material. The synthetic home-solar module is an interface fixture, not an extraction benchmark, learning-resource evaluation, or electrical-engineering recommendation.

Author names, affiliations, and email addresses on the title page are personal information and should remain in a public artifact only with each author's explicit consent. Funding, conflicts of interest, and detailed author contributions cannot be inferred from repository evidence and must be completed by the authors before external submission; absence of repository evidence must not be read as a declaration of none. AI-assisted tools were used for code inspection, consistency checking, and language editing during preparation of this working draft. They did not supply empirical results, and the authors remain responsible for every claim, citation, and released artifact.

\section{Design Discussion}

\subsection{Why Use TypeScript Across the Pipeline}

The repository uses a TypeScript-first production pipeline because the shared contracts are consumed by the pipeline, server, and viewer. Keeping models in \code{packages/types} reduces duplicate definitions and makes it easier for the server and frontend to agree on public API shapes. This does not make Python irrelevant for research scripts or experiments, but the production extraction and serving path benefits from a single typed contract.

\subsection{Why Keep PostgreSQL as the Structured Authority}

File artifacts remain useful for large content, cached markdown, images, and local run logs. They are not reliable as the authoritative index for a multi-stage extraction system. PostgreSQL gives the pipeline and server a common place to store dataset identity, outlines, source status, staged rows, canonical graph rows, pipeline job state, review decisions, and API query state.

\subsection{Why Separate Cards, Bodies, and Source Fragments}

The project draws a strict line among three forms of text:

\begin{itemize}
  \item source fragments: textbook evidence and original context;
  \item node cards: structured summaries and sections;
  \item node bodies: persistent explanatory knowledge prose generated or maintained from evidence.
\end{itemize}

Mixing these would make the system easier to demo but harder to trust. A tutor or graph viewer must know whether it is showing original textbook material, a model-written explanation, or a structured summary.

\subsection{Why Use a Reducer Boundary}

Lesson workers operate with local context and limited evidence. They are allowed to generate candidates. They should not decide global identity, cross-lesson duplicate resolution, or final graph status. The reducer boundary protects the canonical graph from local model noise and gives the project a clear place for merge review, remapping, normalization, and quality policy.

The same boundary applies to cross-domain discovery. Shared names or bridge tags can efficiently retrieve candidates, but they cannot determine identity or factual relations. Candidate scanning therefore remains non-canonical; human evidence review and a transactional reducer are required before any identity merge or relation write.

\section{Limitations}

The current system should be read as an infrastructure substrate, not as a completed AI-native learning platform.

\begin{enumerate}
  \item Object-level lexical retrieval and citation-identifier validation are implemented. Vector and hybrid execution depend on configured query embeddings and stored vectors, while semantic planning, relation-path reasoning, runtime policy, and longitudinal feedback are not complete.
  \item AI Tutor behavior is not yet implemented as a first-class application over \code{ApiUnit}.
  \item Knowledge Realization is still future work: the same Knowledge Object is not yet systematically rendered into exercises, simulations, videos, tutor responses, or teacher materials.
  \item The tracked repository does not yet include an adjudicated multi-subject benchmark, completed human scoring, or reproducible baseline comparisons against chunk retrieval or chapter-centric extraction. Existing local pilot and ablation scaffolds are not sufficient evidence for comparative claims.
  \item Learning outcomes have not been evaluated through learner studies, controlled classroom deployments, or long-term A/B tests.
  \item Graph density and relation quality need more evaluation. The physics run showed useful output, but also isolated nodes and disconnected components.
  \item Interdisciplinary discovery currently uses interpretable lexical, semantic-key, domain, and bridge-tag signals. It does not prove semantic entailment, and no multi-subject, multi-reviewer gold standard yet establishes candidate precision, recall, evidence sufficiency, or review-time savings.
  \item Evidence conflict management, mature multi-expert adjudication, version governance, and learner feedback write-back are not yet complete.
  \item Curriculum mapping, exam weighting, and learner-adaptive use of domain profiles remain planned capabilities.
  \item Generated school-stage pedagogical profiles remain pending review. Evidence-identifier membership, schema checks, and provenance metadata do not establish factual entailment, instructional quality, or learning effectiveness.
  \item Image review can be improved with cheap rule-based prefilters before vision-model calls.
  \item The top-level AI-NKS standard still needs a careful migration path before new runtime relations or governance fields become executable schema.
\end{enumerate}

\section{Future Work}

The next phase should focus on turning the current early runtime into an evaluated and governable learning runtime. Five directions are especially important.

\textbf{Stabilize \code{ApiUnit} as the public contract.}
Existing retrieval and grounded-generation modules already consume \code{ApiUnit}; future tutoring and planning modules should preserve this boundary instead of joining tables directly. This keeps evidence, body, card, relation, and media semantics consistent across applications. The next contract-level additions should include unit export, explicit context-pack versioning, and repair workflows driven by the existing completeness signals.

\textbf{Evaluate and extend object-level retrieval and grounded generation.}
Current search returns complete units, and current generation checks citation identifiers against retrieved evidence and can report insufficient context. The next step is to add semantic support checks, relation-path retrieval, multi-step planning, calibrated refusal, conflict-aware evidence selection, and controlled comparisons with passage retrieval. This direction extends retrieval-augmented generation from passage retrieval toward knowledge-unit retrieval and graph-aware grounding \cite{gao-rag-survey,pan-llm-kg}.

\textbf{Improve graph construction quality.}
The pipeline should improve relation extraction, cross-chunk merge evidence, and graph integrity feedback. The implemented interdisciplinary workflow should be evaluated on an adjudicated multi-subject set before adding vector recall or model reranking; shared labels must remain discovery signals rather than automatic edge evidence. Isolated nodes and weakly connected components are useful diagnostic signals, not merely warnings to suppress.

\textbf{Add knowledge realization and governance.}
The same Knowledge Object should eventually support multiple governed expressions: textbook explanation, tutor response, exercise, diagram, simulation, and teacher material. This requires versioning, expert review, conflict handling, and feedback records rather than one-off generation.

\textbf{Close the education loop.}
The long-term AI-NKS direction needs learner tasks, misconception detection, diagnostic questions, assessment tasks, feedback write-back, and evaluation metrics. It also changes what a course can mean computationally. A traditional course is usually a pre-authored sequence of chapters and lessons; in AI-NKS, a course can become a generated path over a Knowledge Object Network, constrained by learning goals, learner state, evidence availability, curriculum boundaries, and assessment feedback. The current data model and pipeline can store and generate pending-review school-stage pedagogical profiles; the runtime still needs to validate and use them in tutoring and measurement. This future work is motivated by the long-standing tutoring, instructional-design, learning-analytics, and educational-knowledge-graph literature: one-to-one tutoring remains a high bar for learning systems, and effective instruction depends on task-centered practice, feedback, assessment, adaptation to learner state, and domain structures that can support pedagogical applications \cite{bloom-two-sigma,merrill,anderson-krathwohl,vanlehn,koedinger-kli,siemens-learning-analytics,qu-education-kg}.

\section{Conclusion}

\OKM{} is best understood as an implemented knowledge-infrastructure substrate rather than a simple textbook parser or graph viewer. Its core contribution is the disciplined separation of source resources, staged model output, canonical world knowledge, consumption-side knowledge units, generated bodies, and evidence. The current TypeScript workspaces, PostgreSQL schema, Hono API, React viewer, and documented pipeline commands make this separation operational.

The project is deliberately incomplete in the areas that require stronger empirical evidence. It has enough structure to support extraction, inspection, and QA; it already exposes complete units to retrievers and citation-identifier-validated generators; and it records enough evidence and provenance to keep model prose distinct from source material. The next research step is to evaluate this runtime against explicit baselines, extend it to planning and tutoring, and measure whether object-level, evidence-grounded knowledge infrastructure improves accuracy, traceability, maintainability, and learning outcomes compared with ordinary document-chunk retrieval.

\newpage

\appendix

\section{Prompt Inventory}
\label{app:prompt-inventory}

This appendix records the prompt-bearing paths found in the repository source code. It includes fixed prompt text, prompt blocks loaded from extraction templates, and dynamic wrappers that are sent to model APIs. It excludes test fixtures, UI labels, documentation prose, and type fields named \code{prompt} when they are not sent to a model call. The executable source remains normative if later code changes diverge from this snapshot; \code{docs/prompt-inventory.md} is the maintained repository-level inventory.

\begin{table}[H]
\centering
\small
\begin{tabularx}{\textwidth}{@{}p{0.12\textwidth}p{0.32\textwidth}Y@{}}
\toprule
ID & Purpose & Source\\
\midrule
P1a & Lesson/chunk node and evidence extraction & \code{packages/pipeline/src/extraction/model-lesson-extraction.ts}\\
P1b & Lesson/chunk relation extraction & \code{packages/pipeline/src/extraction/model-lesson-extraction.ts}\\
P1c & Subject extraction-template blocks appended to P1 & \code{packages/pipeline/src/extraction/extraction-template.ts}; \code{schemas/extraction-templates/textbook/*.yaml}\\
P1d & Optional project prompt appended to P1 & \code{--prompt} in \code{extract-lesson-openai.ts} and \code{parallel-batch.ts}\\
P2 & Textbook image relevance classification & \code{packages/pipeline/src/extraction/image-relevance.ts}\\
P3 & Knowledge node body generation & \code{packages/pipeline/src/unit-bodies/generate-node-bodies.ts}\\
P4 & Curriculum-projection pedagogical-profile generation & \code{packages/pipeline/src/pedagogical-profiles/generate-pedagogical-profiles.ts}\\
P5 & Runtime grounded generation over retrieved \code{ApiUnit} context & \code{packages/server/src/runtime/grounded-generation.ts}\\
P6 & Staging-quality retry repair prompt appended to P1 & \code{packages/pipeline/src/cli/server-pipeline-run.ts}\\
\bottomrule
\end{tabularx}
\caption{Prompt-bearing paths included in the appendix.}
\label{tab:prompt-inventory}
\end{table}

\subsection{P1: Lesson/Chunk Extraction}

P1 is sent as model instructions. In the Responses API path it is placed in the request-level \code{instructions} field; in the Chat Completions path it is sent as the system message. The user payload is serialized JSON containing the current lesson or chunk context and markdown lines.

\paragraph{P1a: node and evidence extraction base prompt.}
\begin{prompttranscript}
你是 Open Knowledge Map 项目的第一阶段教材知识抽取器。\par
任务是只从当前 lesson/chunk 中抽取证据和候选知识节点。\par

硬约束：\par
1. 只输出 \code{lesson\_disposition}、\code{no\_knowledge\_reason}、\code{nodes}、\code{evidence\_units}、\code{issues} 五个字段。\par
2. 这一阶段绝对不要输出关系；关系会在第二阶段单独判断。\par
3. 每个节点必须能被当前 lesson 的 \code{evidence\_units} 支撑，证据不足就不要列为节点。\par
4. \code{evidence\_units.anchor} 必须稳定、简短、唯一，例如 \code{ev1}、\code{ev2}。\par
5. \code{node.id} 必须稳定、唯一，后续关系阶段会直接引用这些 id。\par
6. 节点主类只能使用 9 类：\par
\code{entity}、\code{concept}、\code{property}、\code{process}、\code{event}、\code{method}、\code{rule}、\code{representation}、\code{resource}。\par
7. 不要把章节编号、复习题、术语表、小结当成正式知识节点。\par
8. 课标是边界，教材是证据，考点是评价，目录是线索；不要把它们直接等同于知识节点。\par
9. 正式候选节点应具备稳定知识身份、证据锚点、关系潜力、教学用途和未来复用性。\par
10. \code{lesson\_context.enrich\_hints} 只是对应教材位置的辅助判断材料，只能帮助判断术语边界、命名和粒度，不能作为节点证据。\par
11. 如果 \code{enrich\_hints} 和当前 lesson/chunk 证据冲突，以当前 lesson/chunk 的证据为准。\par
12. 如果当前课时存在至少一个证据充分、符合准入条件的知识对象，\code{lesson\_disposition} 必须为 \code{extracted}，\code{no\_knowledge\_reason} 必须为空字符串。\par
13. 只有当前课时确实没有任何可抽取知识对象时，\code{lesson\_disposition} 才能为 \code{no\_knowledge}；此时 \code{nodes} 和 \code{evidence\_units} 必须都为空，并在 \code{no\_knowledge\_reason} 中写明原因。\par
14. 不要为了避免空结果而把目录、栏目、题型、活动标题或证据不足的词语提升为节点。\par
15. 输出必须严格符合 JSON schema。
\end{prompttranscript}

\paragraph{P1b: relation extraction base prompt.}
\begin{prompttranscript}
你是 Open Knowledge Map 项目的第二阶段关系抽取器。\par
任务是只根据第一阶段给出的 \code{candidate\_nodes} 和 \code{evidence\_units} 判断关系。\par

硬约束：\par
1. 只输出 \code{edges}、\code{issues} 两个字段。\par
2. 不要新增节点，不要改写节点 id，不要新增证据。\par
3. \code{edge.from} 和 \code{edge.to} 必须来自 \code{candidate\_nodes.id}。\par
4. \code{edge.evidence\_anchor} 必须完全等于 \code{evidence\_units.anchor} 中的一个值。\par
5. 关系 \code{type} 只能来自 \code{allowed\_edge\_types}。\par
6. 如果证据不能直接支持关系，就不要输出该关系。\par
7. 优先抽取教材明确表达的类属、组成、性质、因果、依赖、表示、使用、产出关系。\par
8. 如果 \code{lesson\_context.enrich\_hints} 存在，它只能帮助理解课时主题，不能作为关系证据。\par
9. 输出必须严格符合 JSON schema，不要解释。
\end{prompttranscript}

\paragraph{P1c: extraction-template wrapper.}
When a textbook extraction template is selected, the code appends a generated block to P1. The wrapper has the following shape, with the stage-specific sentence depending on whether the call is in the node/evidence stage or the edge stage:

\begin{prompttranscript}
教材抽取模板：\{template.name\}（\{template.id\} v\{template.version\}）\par
\{template.description\}\par
\{本阶段只使用模板中的节点、证据和标识符规则，不输出关系。 或 本阶段只使用模板中的关系、证据和展示标签规则，不新增节点。\}\par

抽取重点：\par
- \{template.prompt.focus\}\par

证据规则：\par
- \{template.prompt.evidence\_rules\}\par

节点阶段继续追加：节点规则、输出字段、标识符规则和节点展示标签。\par
关系阶段继续追加：关系规则、输出字段、标识符规则和关系展示标签。
\end{prompttranscript}

The fixed template prompt fields currently present in \code{schemas/extraction-templates/textbook} are:

\begin{prompttranscript}
通用教材抽取模板。\par
抽取重点：抽取有明确教材证据支撑的概念、对象、属性、过程、方法、规则和表征。优先保留对后续学习有解释价值的知识对象。\par
节点规则：节点名称使用教材中的规范术语，避免章节号、题号、栏目名成为节点。definition 只写短定义，扩展解释放入 properties.semantic\_core。\par
关系规则：只抽取证据能直接支持的关系。无法确定方向时使用 related\_to，避免编造因果。\par
证据规则：evidence excerpt 保留最能支持节点或关系的一小段教材原文。\par

物理教材抽取模板。\par
抽取重点：优先抽取物理量、物理概念、定律、公式、过程、实验装置、测量方法、图像和模型。关系重点关注因果、影响、依赖、表征、使用、产出、组成和具有性质。对公式、条件、适用范围、变量含义和单位要放入 properties.semantic\_core。\par
节点规则：物理量通常使用 property，公式、定律和守恒关系通常使用 rule。实验步骤和测量方法使用 method，实验装置和器材使用 entity。图像、示意图、矢量图、公式表达和数学模型使用 representation。\par
关系规则：公式表示物理量关系时，优先使用 represents 或 depends\_on。过程导致状态或物理量变化时，优先使用 causes 或 affects。实验方法使用器材、产生数据或验证规律时，使用 uses、produces、represents。\par
证据规则：公式、图像、表格、实验说明都可以作为 evidence，但必须保留 locator 和 modality。\par

化学教材抽取模板。\par
抽取重点：优先抽取物质、微粒、结构、性质、反应、条件、现象、实验操作和化学符号。对反应条件、现象、产物、守恒、结构性质关系要放入 properties.semantic\_core。关系重点关注物质组成、结构决定性质、条件影响反应、方法使用试剂或仪器。\par
节点规则：具体物质、微粒、仪器和样品使用 entity。物质性质使用 property，反应或变化过程使用 process，化学方程式和规则使用 rule 或 representation。实验操作和检验方法使用 method。\par
关系规则：反应物到产物使用 produces，条件对反应的作用使用 affects 或 depends\_on。结构与性质之间优先使用 has\_property、affects 或 depends\_on。实验方法和试剂、仪器之间使用 uses。\par
证据规则：化学方程式、实验现象、表格和图示都是关键证据，不能只保留普通文字说明。\par

生物教材抽取模板。\par
抽取重点：优先抽取生物结构、功能、层级、生命过程、调控机制、适应关系和实验观察。关系重点关注结构与功能、组成层级、调控影响、过程依赖条件和模型表征。对层级范围、条件、例外、常见误解放入 properties.semantic\_core。\par
节点规则：细胞、组织、器官、个体、种群等对象使用 entity。生理过程、调控过程、生态过程使用 process。功能、性状和特征使用 property。实验方法、观察方法和探究步骤使用 method。\par
关系规则：结构与功能之间优先使用 has\_property、affects 或 depends\_on。层级关系使用 part\_of、contains、is\_a 或 instance\_of。调控、反馈和环境影响使用 affects 或 causes。\par
证据规则：图示、模式图、实验记录和数据表可以作为核心 evidence。
\end{prompttranscript}

\paragraph{P1d: optional appended project prompt.}
If a command passes \code{--prompt}, the trimmed text is appended after P1 and any extraction-template block:

\begin{prompttranscript}
补充项目提示：\par
\{用户通过 \code{--prompt} 传入的内容\}
\end{prompttranscript}

\subsection{P2: Textbook Image Relevance}

P2 is sent with an image payload. The fixed instruction text is followed by dynamic textbook context and evidence metadata.

\begin{prompttranscript}
判断这张教材图片是否应该作为知识证据保留。请同时依据图片内容和图片在教材中的上下文判断。\par
核心规则：只要图片内容能和标题、前文、图片行、后文中的任一处形成合理对应，就保留，返回 \code{keep=true}。\par
保留为 \code{core\_content}：图片直接表达概念、结构、实验、数据、流程、地图、模型、例题或主要结论。\par
保留为 \code{supporting}：图片与上下文有关，但只是辅助说明、局部截图、照片、器材、场景、过程片段、例题配图或补充材料。\par
不要因为图片不是最核心、信息量一般、只起辅助作用就返回 \code{uncertain}；这类情况应当返回 \code{keep=true}、\code{relevance="supporting"}。\par
过滤为 \code{decorative}：栏目图标、提示语、页眉页脚、二维码、标志、纯装饰图，且图片本身没有可用的学科知识内容。\par
过滤为 \code{mismatch}：图片本身有知识内容，但和当前标题、前后文或课时明显不相关。\par
只有图片看不清、上下文缺失且图片内容也无法辨认，或确实无法判断图片是否相关时，才返回 \code{keep=true}、\code{relevance="uncertain"}。\par
必须先识别图片内容，再判断相关性。\par
\code{visual\_summary} 写图片中可见的主要内容，不要照抄上下文；如果图片看不清，就说明看不清。\par
\code{reason} 写为什么保留或删除，必须结合图片内容和教材上下文。\par
只返回 JSON：\code{visual\_summary}、\code{keep}、\code{relevance}、\code{reason}、\code{confidence}。\par

教材上下文：标题路径、源文件、源文件行、前文、图片行、后文。\par
证据元数据：图片说明、图片路径、证据原文、位置、尺寸。
\end{prompttranscript}

\subsection{P3: Knowledge Node Body Generation}

P3 writes a durable markdown body for one knowledge node from the node row, structured card, and evidence rows. The user payload is serialized JSON containing the node, card, evidence, and allowed source references.

\begin{prompttranscript}
你是 Open Knowledge Map 的知识正文写作者。\par
任务：根据一个知识节点、它的高质量结构化卡片、课本原文片段和证据引用，写出可阅读、可追溯的 Markdown 知识正文。\par

硬约束：\par
1. 只能使用输入里给出的节点信息、卡片内容和证据片段，不要补充没有证据支持的新事实。\par
2. 正文不是课本原文搬运；可以解释和组织，但不能虚构。\par
3. \code{source\_refs} 只能填写输入 evidence 中出现的 id，不能创造新的证据 id。\par
4. 如果证据不足以支持某个小节，就省略该小节，不要硬写。\par
5. 如果在正文句末标注证据，直接用完整证据 ID 加方括号，例如：\par
\code{[evidence:auto-64c1ee9124ae]}\par
不要用反引号包裹证据标记。\par
6. Markdown 不要使用一级标题；正文可以包含定义、核心解释、关键要点、示例或应用、易错点等二级标题。\par
7. 输出必须是 JSON，不要输出额外解释。
\end{prompttranscript}

\subsection{P4: Curriculum-Scoped Pedagogical Profile Generation}

P4 runs after canonical normalization and knowledge-body generation. It receives one canonical node, one Curriculum Projection with its domain and school-stage context, the node card, neighboring relations and nodes, linked evidence, and the allowed evidence identifiers. Domain Profile semantics may be supplied as context, but the generated teaching data is written only to the Curriculum Projection. The fixed instruction text is:

\begin{prompttranscript}
你是 Open Knowledge Map 的教学画像生成器。\par
任务：根据一个已经规范化的知识对象、指定领域和指定学段，生成可用于教学、诊断和评价的结构化画像。\par

硬约束：\par
1. 知识事实只能来自输入的节点、结构化卡片、关系和证据；不得补充无证据支持的学科事实。\par
2. 学习目标、问题、任务和建议可以进行教学设计，但必须围绕输入知识，不得引入新的知识结论。\par
3. 每个列表写 1 至 3 条，内容具体、简洁、可执行，避免空泛表述。\par
4. 学习目标使用可观察的行为描述；评价任务应能检验这些目标。\par
5. 难度必须相对于当前 \code{school_stage} 判断，只能使用 \code{introductory}、\code{basic}、\code{intermediate}、\code{advanced}、\code{expert}。\par
6. \code{source_refs} 只能填写 \code{allowed_source_refs} 中出现的证据编号，不能创造编号。\par
7. \code{common_errors} 只描述与当前知识边界直接相关的典型错误，不要编造教材没有涉及的事实。\par
8. 输出必须严格符合 JSON schema，不要输出额外解释。
\end{prompttranscript}

The response schema has nine required keys covering learning objectives, stage-relative difficulty, diagnostic questions, common errors, assessment tasks, remediation and extension suggestions, source references, and confidence. The application verifies schema shape and evidence-identifier membership, records generation provenance, and stores the result with \code{review_status=pending}; it does not independently prove that each teaching statement is entailed by the cited excerpt.

\subsection{P5: Runtime Grounded Generation}

P5 is the server-side grounded-generation prompt. It retrieves \code{ApiUnit} records, serializes a bounded context, and asks the model to answer in JSON.

\begin{lstlisting}
You answer questions only from the provided Open Knowledge Map ApiUnit context.
Use the same language as the user question.
Every factual claim should be grounded in the context.
Citations must use evidence_id values that appear in the context.
If the context is insufficient, say so and leave citations empty.
Return JSON only with keys: answer, citations, unsupported_claims, used_node_ids.
Each citation must be { "node_id": string, "evidence_id": string, "note": string }.
\end{lstlisting}

The corresponding user message wrapper is:

\begin{lstlisting}
Question:
{question}

ApiUnit context:
{context}
\end{lstlisting}

The generated context contains bounded unit blocks with fields such as:

\begin{lstlisting}
Unit {index}
node_id: {node.id}
name: {node.name}
kind: {node.kind}
definition: {node.definition}
aliases: {node.aliases}
semantic_core: {node.properties.semantic_core}
card_summary: {unit.card.summary}
card_sections:
{section.title}: {section.content}
body:
{unit.body.content}
relations:
{relation summary}
evidence:
evidence_id: {evidence.id} locator={locator} page={page}
{evidence.excerpt}
source_fragments:
Source fragment {source_id}:{anchor_ref}
{fragment excerpts}
\end{lstlisting}

\subsection{P6: Staging-Quality Retry Prompt}

P6 is generated only after staging-quality blocks a lesson/chunk. It is appended through \code{--prompt} for the affected retry command.

\begin{prompttranscript}
这是第 \{retryNumber\} 次质量失败后的自动重抽。\par
上一轮问题：\{issueText\}\par
请重新核对当前 chunk 是否包含与\{subjectContext\}对应、且有直接教材证据支撑的知识对象。\par
第一阶段只按当前 schema 返回 \code{lesson\_disposition}、\code{no\_knowledge\_reason}、\code{nodes}、\code{evidence\_units} 和 \code{issues}；不要要求或输出当前阶段不支持的画像、提及或卡片字段。\par
如果存在合格知识对象，\code{lesson\_disposition} 设为 \code{extracted}，并让每个节点通过 \code{evidence\_units.node\_ids} 绑定自己的证据；如果确实没有合格知识对象，\code{lesson\_disposition} 设为 \code{no\_knowledge}，所有知识产物保持为空，并明确填写 \code{no\_knowledge\_reason}。\par
第二阶段只为第一阶段的节点抽取证据充分的关系；证据不足时允许关系为空，不要为了通过检查而制造节点或关系。
\end{prompttranscript}

\newpage

\section{Schema and API Contract Summary}
\label{app:schema-api}

This appendix summarizes the executable schema and service contract used by the current repository. It is not a complete restatement of the broader \AINKS{} theory. The executable layer is \code{world-v1.3}; the public unit contract exposed to applications is \code{ApiUnit}.

\subsection{Executable World Schema}

\code{world-v1.3} follows five engineering rules. First, a Knowledge Object has one canonical identity and one controlled top-level kind. Second, disciplinary semantics, curriculum placement, relations, and evidence are stored separately. Third, each discipline validates roles through its own Domain Schema rather than one universal subject-role list. Fourth, cross-domain meaning uses explicit bridge objects and evidence-backed path segments. Fifth, generated explanatory content is not treated as source evidence.

\begin{table}[H]
\centering
\small
\begin{tabularx}{\textwidth}{@{}p{0.26\textwidth}Y@{}}
\toprule
Schema element & Current executable rule\\
\midrule
Node kinds & The legal \code{world_nodes.kind} values are \code{entity}, \code{concept}, \code{property}, \code{process}, \code{event}, \code{method}, \code{rule}, \code{representation}, and \code{resource}.\\
Semantic core & Rich semantic fields are stored under \code{world_nodes.properties_json.semantic_core}, including \code{core_claims}, \code{formal_expressions}, \code{conditions}, \code{boundaries}, \code{counterexamples}, and \code{misconceptions}.\\
Relation types & The eighteen active codes are \code{is_a}, \code{instance_of}, \code{part_of}, \code{contains}, \code{has_property}, \code{uses}, \code{produces}, \code{depends_on}, \code{prerequisite_for}, \code{causes}, \code{affects}, \code{represents}, \code{formalizes}, \code{applies_to}, \code{analogous_to}, \code{models}, \code{about}, and \code{related_to}. Chinese names and sentence templates are part of the shared contract; codes remain stable internal identifiers.\\
Identity boundary & The retired \code{same_as} value is rejected for new writes. Confirmed identity equivalence is represented by canonical node merge and reference remapping.\\
Multidisciplinary boundary & Cross-domain scans write governance candidates only. A reviewed direct relation must select one active type, direction, and eligible evidence. A bridge path must use an explicit bridge object and separately reviewed evidence for each segment before the reducer writes the single canonical \code{world_edges} table.\\
Domain Profile & \code{world_domain_profiles} stores a domain, Domain Schema id and version, one legal discipline-specific role, semantic properties, and source references. It does not store curriculum or teaching placement.\\
Curriculum Projection & \code{world_curriculum_projections} stores curriculum id, domain, school stage, grade band, curriculum roles, source references, and an optional \code{properties_json.pedagogical_profile}.\\
Source policy & \code{world_source_policies} defines relation-evidence eligibility, explicit-review requirements, and trust tier for each governed source type.\\
Evidence boundary & Evidence is stored in \code{world_evidence} and connected through mentions, relation source references, cards, bodies, and evidence links. Model-generated prose must cite evidence; it is not itself textbook evidence.\\
\bottomrule
\end{tabularx}
\caption{Core executable rules in \code{world-v1.3}.}
\label{tab:world-v13-rules}
\end{table}

\begin{table}[H]
\centering
\small
\begin{tabularx}{\textwidth}{@{}p{0.27\textwidth}Y@{}}
\toprule
Table or table group & Role in the executable schema\\
\midrule
\code{world_nodes} & Stable object identity: id, name, kind, definition, aliases, domains, knowledge form, learning mode, scope, semantic properties, tags, embedding, status, and deprecation boundary.\\
\code{world_edges} & The single canonical relation table: active relation code, Chinese display metadata derived at the service layer, endpoints, directionality, relation scope, confidence, evidence references, properties, status, and notes.\\
\code{world_domain_schemas} and \code{world_domain_profiles} & Versioned discipline-specific legal roles and each object's semantic role and properties in a domain, without changing its canonical identity or top-level kind.\\
\code{world_curriculum_projections} & Curriculum, stage, grade, teaching-role, and pedagogical projection for an object. Generated profiles include teaching fields, source identifiers, model and prompt provenance, input fingerprint, confidence, and review state.\\
\code{world_source_policies} & Governed source classes and their evidence-use rules.\\
\code{world_mentions} & Source-level mention records that connect a source location to a node, edge, taxonomy term, or domain profile.\\
\code{world_evidence} & Source-backed evidence units with source id, anchor, source path, page range, excerpt, locator, modality, extraction method, normalized claims, and properties.\\
\code{world_evidence_links} & Additional evidence attachment table for edges, domain profiles, curriculum projections, mentions, node cards, and node-card sections.\\
\code{world_node_cards} & Structured inspectable summaries for nodes, including card sections and evidence references.\\
\code{world_node_bodies} & Persistent markdown knowledge bodies with media references, evidence references, generation provenance, and status.\\
\code{world_unit_embeddings} & Complete-unit retrieval text, 1024-dimensional embedding, content hash, embedding-model provenance, and generation timestamp keyed by data set and node.\\
\code{world_lesson_runs} and \code{world_staging_*} & Lesson/chunk-scoped staging output. Model workers write here before canonical merge and QA.\\
Pipeline runtime tables & \code{world_pipeline_jobs}, \code{world_pipeline_stages}, \code{world_pipeline_events}, and \code{world_pipeline_workers} store server-launched pipeline state, progress, logs, events, and worker status.\\
Multidisciplinary governance tables & \code{world_interdisciplinary_runs} stores scan configuration and statistics; \code{world_interdisciplinary_candidates} stores node-alignment, direct-relation, and bridge-path proposals, segment evidence, human review, and application state. These rows are not \code{ApiUnit} relations.\\
\code{retrieval_candidates} & Candidate context associated with extraction batch anchors and query ids; the runtime's complete-unit vector path uses \code{world_unit_embeddings}.\\
\bottomrule
\end{tabularx}
\caption{Main storage responsibilities in the PostgreSQL schema.}
\label{tab:world-storage-summary}
\end{table}

\subsection{ApiUnit Contract}

\code{ApiUnit} is the public service-layer response for a complete knowledge unit. It is assembled by the server from canonical tables; consumers should not treat a row in \code{world_nodes} as a complete knowledge point.

\begin{table}[H]
\centering
\small
\begin{tabularx}{\textwidth}{@{}p{0.25\textwidth}Y@{}}
\toprule
\code{ApiUnit} field & Meaning\\
\midrule
\code{node} & Normalized object identity: id, dataset id, name, kind, subkind, definition, aliases, domains, knowledge form, learning mode, scope, properties, external ids, tags, status, deprecation target, timestamps, and notes.\\
\code{relations.outgoing} and \code{relations.incoming} & Typed graph neighborhood around the object, split by edge direction and including the stable code, Chinese label, confidence, source references, properties, status, and notes.\\
\code{domain_profiles} & Discipline-specific semantic views: Domain Schema id/version, domain role, semantic properties, source references, and status.\\
\code{curriculum_projections} & Teaching placement: curriculum, stage, grade, curriculum roles, source references, and the projection's optional pedagogical profile.\\
\code{mentions} & Source mentions that link the object to textbook or resource locations.\\
\code{evidence} & Source-backed evidence rows supporting the object. Hidden image evidence is filtered from ordinary unit output when it is rejected, pending, or unresolved uncertain evidence.\\
\code{media} & Reviewed image media derived from evidence rows, including URL, path, caption, evidence id, source id, anchor, and page range.\\
\code{source_fragments} & Grouped source fragments assembled from evidence and textbook outline information, preserving source id, anchor, modality set, and excerpts.\\
\code{card} & Structured node card, or \code{null} when missing. The card gives inspectable summary sections and evidence references.\\
\code{body} & Persistent markdown body, or \code{null} when missing. The body is generated or curated explanatory text and must carry \code{source_refs}.\\
\code{completeness} & Unit-completeness score and per-signal pass/fail details. This is a service-layer diagnostic, not a claim that the object is pedagogically complete.\\
\bottomrule
\end{tabularx}
\caption{\code{ApiUnit} fields and their meanings.}
\label{tab:apiunit-fields}
\end{table}

The unit-completeness diagnostic currently checks nine signals:

\begin{table}[H]
\centering
\small
\begin{tabularx}{\textwidth}{@{}p{0.25\textwidth}p{0.18\textwidth}Y@{}}
\toprule
Signal & Severity & What is checked\\
\midrule
\code{node_definition} & Required & The node has a readable definition.\\
\code{semantic_core} & Recommended & At least one semantic-core group is populated.\\
\code{relations} & Recommended & The node has at least one incoming or outgoing relation.\\
\code{evidence} & Required & The unit has evidence rows.\\
\code{source_fragments} & Recommended & The unit can show source fragments.\\
\code{domain_profiles} & Required & The unit has domain, school-stage, or curriculum-role context.\\
\code{body_source_refs} & Recommended & The persistent body exists and carries source references.\\
\code{card_summary} & Recommended & The structured card has a summary or sections.\\
\code{mentions} & Required & The node has source mention records.\\
\bottomrule
\end{tabularx}
\caption{\code{ApiUnit} completeness signals.}
\label{tab:apiunit-completeness}
\end{table}

The implemented retrieval, grounded-generation, and streaming response contracts are summarized in Section~\ref{sec:server-runtime} and share \code{ApiUnit} as their context boundary. They form an early runtime subset; semantic planning and broad benchmark evaluation remain future work.

\newpage

\section{Quality Evaluation and Review Details}
\label{app:quality-evaluation}

The repository uses several quality layers because no single check can cover extraction quality, schema legality, graph structure, source grounding, and human review. The current quality system is operational rather than final empirical validation.

\subsection{Quality Gates}

\begin{table}[H]
\centering
\small
\begin{tabularx}{\textwidth}{@{}P{0.22\textwidth}YP{0.22\textwidth}@{}}
\toprule
Gate & What it checks & Blocking behavior\\
\midrule
\code{staging-quality} & Extracted lesson/chunk output: nodes and evidence, valid kinds, definitions, profiles, mentions, cards, evidence-backed references, edge legality, and required card sections. An explicit \code{no_knowledge} result instead requires a reason and zero staged artifacts. Synthetic or quality-excluded evidence cannot satisfy coverage. & Blocks affected runs unless invoked in warning-only mode. Non-blocking warnings and review-node ids are persisted; blocked chunks can be retried with a schema-aligned repair prompt.\\
\code{strict-qa} & Canonical schema and evidence-identifier legality: valid node kinds, domains, Domain Schema roles, learning modes, edge types, directionality, evidence references, semantic Domain Profiles, Curriculum Projections, projection-scoped pedagogical-profile shape and provenance, mentions, cards, card sections, node body format, body source references, and media references. & Returns \code{blocked} when errors exist.\\
\code{graph-integrity} & Canonical graph structure: hard cycles over hierarchical relations, directed-cycle warnings, isolated nodes, and large weakly connected components. & Succeeds by default while reporting warnings; can block on hard cycles when \code{--fail-on-cycles} is used.\\
\code{quality-dashboard} & Operational metrics: lesson counts, node counts, relation counts, evidence coverage, isolated-node ratio, disconnected components, pending image reviews, merge-review items, interdisciplinary-review items, blocked lessons, and manual pending items. & Does not replace QA gates; it exposes inspection and triage metrics.\\
Manual review & Human decisions for uncertain image evidence, merge-review cases, and interdisciplinary candidates; relation approval requires selected existing textbook evidence. & Updates stored review state or guides reducer application and follow-up reruns.\\
\bottomrule
\end{tabularx}
\caption{Quality gates used by the current pipeline.}
\label{tab:quality-gates}
\end{table}

The main commands are:

\begin{lstlisting}[language=bash]
npm run staging-quality -w packages/pipeline -- --dataset-id main --db "$DATABASE_URL"
npm run strict-qa -w packages/pipeline -- --dataset-id main --db "$DATABASE_URL"
npm run graph-integrity -w packages/pipeline -- --dataset-id main --db "$DATABASE_URL"
npm run quality-dashboard -w packages/pipeline -- --dataset-id main --db "$DATABASE_URL"
\end{lstlisting}

\subsection{Strict QA Details}

\code{strict-qa} checks canonical data after merge and normalization. Its current error categories include \code{node}, \code{edge}, \code{domain_profile}, \code{curriculum_projection}, \code{pedagogical_profile}, \code{mention}, \code{node_card}, \code{node_card_section}, and \code{node_body}. It checks, among other things:

\begin{itemize}
  \item nodes must have legal kinds, names, definitions, non-empty domains, non-empty learning modes, cards, mentions, and domain profiles;
  \item edges must use active types and legal directionality, reference existing endpoints, carry evidence source references, and declare an intra-domain, cross-domain, or universal scope;
  \item domain profiles must reference a valid Domain Schema, use a legal discipline-specific role, avoid teaching-only fields, and carry existing evidence source references;
  \item curriculum projections must declare curriculum, domain, school stage, grade band, curriculum roles, and existing evidence source references;
  \item pedagogical profiles must match their containing curriculum projection and contain legal teaching fields; model-generated entries must record model, prompt version, generation time, input fingerprint, review status, confidence, and existing source references;
  \item mentions, node cards, and card sections must cite evidence that exists in \code{world_evidence};
  \item node cards must contain a summary and required sections: \code{definition}, \code{essence}, \code{key_points}, \code{example}, \code{application}, and \code{misconception};
  \item node bodies must use markdown format, contain content, use a legal \code{generated_from} value, cite source references, and declare media references for local markdown images.
\end{itemize}

\subsection{Graph Integrity Details}

\code{graph-integrity} separates hard structural problems from diagnostic warnings. The hard cycle check is applied to hierarchical edge types: \code{is_a}, \code{instance_of}, \code{part_of}, \code{contains}, \code{depends_on}, and \code{prerequisite_for}. Directed cycle warnings are computed over other directed relation types except the undirected \code{related_to} and \code{analogous_to} relations. Isolated nodes and weakly connected components are reported because they indicate graph-construction gaps even when row-level schema is valid.

This distinction is intentional. A graph can pass schema QA and still be weak as a teaching graph if many nodes are isolated or if large components remain disconnected. Those findings should trigger relation extraction, merge, or normalization improvements; they should not automatically delete nodes.

\subsection{Observed Failure Cases}

The documented physics-textbook run provides concrete failure cases that shaped the current QA design:

\begin{table}[H]
\centering
\small
\begin{tabularx}{\textwidth}{@{}P{0.24\textwidth}YP{0.27\textwidth}@{}}
\toprule
Failure case & What happened & Current mitigation\\
\midrule
Implicit zero-node chunk & \code{chunk:11-4-a} historically returned no nodes without an explicit valid no-knowledge disposition. & Current \code{staging-quality} accepts only reasoned \code{no_knowledge} with no artifacts; other zero-node output is blocked and can be retried without forcing fabricated nodes.\\
Partial staging write & \code{chunk:9-7-a} hit a duplicate mention key and left a partial staging state. & Staging writes now run as a transaction: lesson run upsert, old staging deletion, and new staging inserts succeed or roll back together.\\
Merge failure & An early merge failed because an \code{undefined} value reached PostgreSQL JSONB parameters. & PostgreSQL parameter preparation converts objects and arrays correctly and normalizes \code{undefined} to \code{null}. The failed merge run was superseded by a successful run.\\
Image filtering side effect & Removing decorative image evidence could leave nodes, mentions, profiles, cards, or card sections with empty source references. & Post-processing reconnects remaining text evidence when text evidence is still available.\\
Graph sparsity & The physics run passed strict QA and graph-integrity execution, but still reported 2 directed-cycle warnings, 126 isolated nodes, and 12 disconnected components. & These are treated as graph-quality diagnostics for future relation extraction and cross-chunk merge improvement.\\
\bottomrule
\end{tabularx}
\caption{Observed quality failures and mitigations from the documented physics run.}
\label{tab:observed-failures}
\end{table}

\subsection{Manual Review Standards}

Manual review is used where schema checks cannot decide semantic quality by themselves.

\begin{enumerate}
  \item Node admission: a candidate node should have a stable knowledge identity, a legal kind, a readable definition, evidence support, a mention, a domain profile, and a structured card. Directory headings, exercise labels, assessment labels, and textbook columns should not become nodes merely because they appear in a source.
  \item Evidence review: source references should point to actual \code{world_evidence} rows. Evidence may be text, image, table, formula, or other modality, but generated summaries and generated bodies are not source evidence.
  \item Synthetic evidence review: fallback rows marked \code{synthetic} or \code{quality_excluded} stay in the review queue but do not count toward evidence coverage and cannot make a candidate pass admission checks.
  \item Image review: pending or unresolved uncertain images are hidden from ordinary unit views. A reviewer may mark an image as \code{core_content}, \code{supporting}, \code{keep}, \code{drop}, or \code{uncertain}. The stored review status becomes \code{approved}, \code{rejected}, or \code{pending}.
  \item Pedagogical-profile review: generated objectives, questions, errors, tasks, and recommendations remain pending until a reviewer checks their school-stage fit, factual scope, evidence relevance, internal alignment, and classroom suitability. A valid source identifier alone is not sufficient approval.
  \item Merge review: cases marked for review in the canonical-node map should be judged by comparing names, definitions, semantic fields, and evidence. A merge should not be accepted only because two labels are similar.
  \item Relation review: relation type, direction, and source evidence should be checked together. Weak associations should remain \code{related_to}; causal, prerequisite, structural, or taxonomic edges need stronger direct evidence.
  \item Interdisciplinary review: names, aliases, semantic keys, domains, and bridge tags are candidate signals only. Identity alignment must compare meanings and evidence before merging. Relation approval must select at least one candidate evidence record, then the separate application reducer rechecks current canonical state before writing.
  \item Graph review: cycles in hierarchical relations require correction or an explicit modeling decision. Isolated nodes and disconnected components are signals for missing relations, over-fragmented nodes, or insufficient extraction context.
\end{enumerate}

\subsection{Boundary of Current Evaluation}

The current quality system validates whether a run is structurally usable and evidence-linked enough for inspection. The tracked fixture and evaluator support a small retrieval smoke check. Generation mode provides structural citation-identifier diagnostics, but no adjudicated generation result is reported. Cross-domain smoke checks establish transaction and interface behavior, not interdisciplinary accuracy or completeness. These checks do not prove that the graph is pedagogically optimal, that all relations are semantically correct, that object-level retrieval beats chunk retrieval, or that learners achieve better outcomes. Those claims require external benchmarks, human annotation, baseline comparisons, and learning-outcome evaluation.

\newpage

\begin{thebibliography}{99}

\bibitem{bfo}
Robert Arp, Barry Smith, and Andrew D. Spear.
\newblock \emph{Building Ontologies with Basic Formal Ontology}.
\newblock MIT Press, 2015.

\bibitem{bernstein-classification}
Basil Bernstein.
\newblock On the classification and framing of educational knowledge.
\newblock In Michael F. D. Young, editor, \emph{Knowledge and Control}, pages 47--69. Collier-Macmillan, 1971.
\newblock Full volume title: \emph{Knowledge and Control: New Directions for the Sociology of Education}.
\newblock Reprinted by Routledge, \href{https://doi.org/10.4324/9781351018142-13}{https://doi.org/10.4324/9781351018142-13}

\bibitem{anderson-krathwohl}
Lorin W. Anderson, David R. Krathwohl, Peter W. Airasian, Kathleen A. Cruikshank, Richard E. Mayer, Paul R. Pintrich, James Raths, and Merlin C. Wittrock.
\newblock \emph{A Taxonomy for Learning, Teaching, and Assessing: A Revision of Bloom's Taxonomy of Educational Objectives}.
\newblock Longman, 2001.

\bibitem{bloom-two-sigma}
Benjamin S. Bloom.
\newblock The 2 sigma problem: The search for methods of group instruction as effective as one-to-one tutoring.
\newblock \emph{Educational Researcher}, 13(6):4--16, 1984.

\bibitem{bull-kay}
Susan Bull and Judy Kay.
\newblock SMILI: A framework for interfaces to learning data in open learner models, learning analytics and related fields.
\newblock \emph{International Journal of Artificial Intelligence in Education}, 26:293--331, 2016.

\bibitem{corbett-anderson}
Albert T. Corbett and John R. Anderson.
\newblock Knowledge tracing: Modeling the acquisition of procedural knowledge.
\newblock \emph{User Modeling and User-Adapted Interaction}, 4:253--278, 1994.

\bibitem{comenius-great-didactic}
John Amos Comenius.
\newblock \emph{The Great Didactic of John Amos Comenius}.
\newblock Translated by M. W. Keatinge. Adam and Charles Black, 1896.
\newblock \href{https://archive.org/details/greatdidacticofj00come}{https://archive.org/details/greatdidacticofj00come}

\bibitem{elman-civil-examinations}
Benjamin A. Elman.
\newblock \emph{A Cultural History of Civil Examinations in Late Imperial China}.
\newblock University of California Press, 2000.
\newblock \href{https://www.ucpress.edu/book/9780520215092/a-cultural-history-of-civil-examinations-in-late-imperial-china}{https://www.ucpress.edu/book/9780520215092/a-cultural-history-of-civil-examinations-in-late-imperial-china}

\bibitem{gao-rag-survey}
Yunfan Gao, Yun Xiong, Xinyu Gao, Kangxiang Jia, Jinliu Pan, Yuxi Bi, Yi Dai, Jiawei Sun, Meng Wang, and Haofen Wang.
\newblock Retrieval-augmented generation for large language models: A survey.
\newblock arXiv preprint \href{https://arxiv.org/abs/2312.10997}{arXiv:2312.10997}, 2023.
\newblock \href{https://doi.org/10.48550/arXiv.2312.10997}{https://doi.org/10.48550/arXiv.2312.10997}

\bibitem{hogan-kg}
Aidan Hogan, Eva Blomqvist, Michael Cochez, Claudia d'Amato, Gerard de Melo, Claudio Gutierrez, Jos\'{e} Emilio Labra Gayo, Sabrina Kirrane, Sebastian Neumaier, Axel Polleres, Roberto Navigli, Axel-Cyrille Ngonga Ngomo, Sabbir M. Rashid, Anisa Rula, Lukas Schmelzeisen, Juan Sequeda, Steffen Staab, and Antoine Zimmermann.
\newblock Knowledge graphs.
\newblock \emph{ACM Computing Surveys}, 54(4):1--37, 2021.

\bibitem{humboldt-higher-institutions}
Wilhelm von Humboldt.
\newblock On the internal and external organization of the higher scientific institutions in Berlin.
\newblock 1810.
\newblock Translated by Thomas Dunlap in \emph{German History in Documents and Images}.
\newblock \href{https://germanhistorydocs.org/en/the-holy-roman-empire-1648-1815/ghdi:document-3642}{https://germanhistorydocs.org/en/the-holy-roman-empire-1648-1815/ghdi:document-3642}

\bibitem{ji-hallucination}
Ziwei Ji, Nayeon Lee, Rita Frieske, Tiezheng Yu, Dan Su, Yan Xu, Etsuko Ishii, Ye Jin Bang, Andrea Madotto, and Pascale Fung.
\newblock Survey of hallucination in natural language generation.
\newblock \emph{ACM Computing Surveys}, 55(12), Article 248, 2023.
\newblock \href{https://doi.org/10.1145/3571730}{https://doi.org/10.1145/3571730}

\bibitem{ji-kg-survey}
Shaoxiong Ji, Shirui Pan, Erik Cambria, Pekka Marttinen, and Philip S. Yu.
\newblock A survey on knowledge graphs: Representation, acquisition, and applications.
\newblock \emph{IEEE Transactions on Neural Networks and Learning Systems}, 33(2):494--514, 2022.
\newblock \href{https://doi.org/10.1109/TNNLS.2021.3070843}{https://doi.org/10.1109/TNNLS.2021.3070843}

\bibitem{koedinger-kli}
Kenneth R. Koedinger, Albert T. Corbett, and Charles Perfetti.
\newblock The Knowledge-Learning-Instruction framework: Bridging the science-practice chasm to enhance robust student learning.
\newblock \emph{Cognitive Science}, 36(5):757--798, 2012.
\newblock \href{https://doi.org/10.1111/j.1551-6709.2012.01245.x}{https://doi.org/10.1111/j.1551-6709.2012.01245.x}

\bibitem{merrill}
M. David Merrill.
\newblock First principles of instruction.
\newblock \emph{Educational Technology Research and Development}, 50(3):43--59, 2002.

\bibitem{pan-llm-kg}
Shirui Pan, Linhao Luo, Yufei Wang, Chen Chen, Jiapu Wang, and Xindong Wu.
\newblock Unifying large language models and knowledge graphs: A roadmap.
\newblock \emph{IEEE Transactions on Knowledge and Data Engineering}, 36(7):3580--3599, 2024.
\newblock \href{https://doi.org/10.1109/TKDE.2024.3352100}{https://doi.org/10.1109/TKDE.2024.3352100}

\bibitem{sweller}
John Sweller.
\newblock Cognitive load during problem solving: Effects on learning.
\newblock \emph{Cognitive Science}, 12(2):257--285, 1988.

\bibitem{qu-education-kg}
Kechen Qu, Kam Cheong Li, Billy T. M. Wong, Manfred M. F. Wu, and Mengjin Liu.
\newblock A survey of knowledge graph approaches and applications in education.
\newblock \emph{Electronics}, 13(13):2537, 2024.
\newblock \href{https://doi.org/10.3390/electronics13132537}{https://doi.org/10.3390/electronics13132537}

\bibitem{piech-dkt}
Chris Piech, Jonathan Bassen, Jonathan Huang, Surya Ganguli, Mehran Sahami, Leonidas J. Guibas, and Jascha Sohl-Dickstein.
\newblock Deep knowledge tracing.
\newblock In \emph{Advances in Neural Information Processing Systems}, 2015.

\bibitem{siemens-learning-analytics}
George Siemens.
\newblock Learning analytics: The emergence of a discipline.
\newblock \emph{American Behavioral Scientist}, 57(10):1380--1400, 2013.
\newblock \href{https://doi.org/10.1177/0002764213498851}{https://doi.org/10.1177/0002764213498851}

\bibitem{soysal-strang-mass-education}
Yasemin Nuhoglu Soysal and David Strang.
\newblock Construction of the first mass education systems in nineteenth-century Europe.
\newblock \emph{Sociology of Education}, 62(4):277--288, 1989.
\newblock \href{https://doi.org/10.2307/2112831}{https://doi.org/10.2307/2112831}

\bibitem{skos}
Alistair Miles and Sean Bechhofer.
\newblock \emph{SKOS Simple Knowledge Organization System Reference}.
\newblock W3C Recommendation, 2009.

\bibitem{rdf}
Richard Cyganiak, David Wood, and Markus Lanthaler.
\newblock \emph{RDF 1.1 Concepts and Abstract Syntax}.
\newblock W3C Recommendation, 2014.

\bibitem{owl}
W3C OWL Working Group.
\newblock \emph{OWL 2 Web Ontology Language Document Overview}.
\newblock W3C Recommendation, 2012.

\bibitem{shacl}
Holger Knublauch and Dimitris Kontokostas.
\newblock \emph{Shapes Constraint Language (SHACL)}.
\newblock W3C Recommendation, 2017.

\bibitem{prov}
Timothy Lebo, Satya Sahoo, and Deborah McGuinness.
\newblock \emph{PROV-O: The PROV Ontology}.
\newblock W3C Recommendation, 2013.

\bibitem{rag}
Patrick Lewis, Ethan Perez, Aleksandra Piktus, Fabio Petroni, Vladimir Karpukhin, Naman Goyal, Heinrich K\"uttler, Mike Lewis, Wen-tau Yih, Tim Rockt\"aschel, Sebastian Riedel, and Douwe Kiela.
\newblock Retrieval-augmented generation for knowledge-intensive NLP tasks.
\newblock \emph{Advances in Neural Information Processing Systems}, 2020.

\bibitem{vanlehn}
Kurt VanLehn.
\newblock The behavior of tutoring systems.
\newblock \emph{International Journal of Artificial Intelligence in Education}, 16(3):227--265, 2006.

\bibitem{unesco-ai-students}
Fengchun Miao, Kelly Shiohira, and Natalie Lao.
\newblock \emph{AI Competency Framework for Students}.
\newblock UNESCO, 2024.
\newblock \href{https://www.unesco.org/en/articles/ai-competency-framework-students}{https://www.unesco.org/en/articles/ai-competency-framework-students}

\bibitem{unesco-ai-teachers}
Fengchun Miao and Mutlu Cukurova.
\newblock \emph{AI Competency Framework for Teachers}.
\newblock UNESCO, 2024.
\newblock \href{https://www.unesco.org/en/articles/ai-competency-framework-teachers}{https://www.unesco.org/en/articles/ai-competency-framework-teachers}

\bibitem{wiley-learning-objects}
David A. Wiley.
\newblock Connecting learning objects to instructional design theory: A definition, a metaphor, and a taxonomy.
\newblock In David A. Wiley, editor, \emph{The Instructional Use of Learning Objects}. Association for Educational Communications and Technology, 2000.
\newblock \href{https://www.reusability.org/read/}{https://www.reusability.org/read/}

\bibitem{xi-agent-survey}
Zhiheng Xi, Wenxiang Chen, Xin Guo, Wei He, Yiwen Ding, Boyang Hong, Ming Zhang, Junzhe Wang, Senjie Jin, Enyu Zhou, Rui Zheng, Xiaoran Fan, Xiao Wang, Limao Xiong, Yuhao Zhou, Weiran Wang, Changhao Jiang, Yicheng Zou, Xiangyang Liu, Zhangyue Yin, Shihan Dou, Rongxiang Weng, Wenbin Cheng, Qi Zhang, Wenjuan Qin, Yongyan Zheng, Xipeng Qiu, Xuanjing Huang, and Tao Gui.
\newblock The rise and potential of large language model based agents: A survey.
\newblock \emph{Science China Information Sciences}, 68(2):121101, 2025.
\newblock \href{https://doi.org/10.1007/s11432-024-4222-0}{https://doi.org/10.1007/s11432-024-4222-0}

\bibitem{zhao-llm-survey}
Wayne Xin Zhao, Kun Zhou, Junyi Li, Tianyi Tang, Xiaolei Wang, Yupeng Hou, Yingqian Min, Beichen Zhang, Junjie Zhang, Zican Dong, Yifan Du, Chen Yang, Yushuo Chen, Zhipeng Chen, Jinhao Jiang, Ruiyang Ren, Yifan Li, Xinyu Tang, Zikang Liu, Peiyu Liu, Jian-Yun Nie, and Ji-Rong Wen.
\newblock A survey of large language models.
\newblock \emph{Frontiers of Computer Science}, 20:2012627, 2026.
\newblock \href{https://doi.org/10.1007/s11704-026-60308-3}{https://doi.org/10.1007/s11704-026-60308-3}

\end{thebibliography}

\end{document}
